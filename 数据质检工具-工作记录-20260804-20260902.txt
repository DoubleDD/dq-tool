2026-09-02 周三
数据质检工具
	新增 SQL 控制台：选择数据源执行任意 SQL 并查看结果。后端在 common 内核新增 SqlConsoleService，原文透传 JDBC 执行，查询返回列名+行数据（上限 1000 行截断并标记 truncated），非查询返回受影响行数，超时口径与数据预览一致；前端新增 SqlConsole 页面（CodeMirror 6 编辑器，Ctrl/Cmd+Enter 优先执行选中片段），历史记录存 localStorage 最近 50 条不落服务端。配套 4 个单测与端点冒烟用例，并新建 SQL控制台 wiki 文档。
	抽样导出支持按 Excel「数据量」列逐表控制抽样条数：导入模版新增可选列，留空/缺列/非数字一律按默认 50 条，旧表格无该列同口径兼容；新增 V26 迁移 sample_export_item.sample_limit 列，明细抽屉同步展示。
	扫描任务记录数据库版本号：scan_job 新增 db_version 列（V25 迁移），任务创建时从连接元数据快照目标库版本，扫描详情页展示，扫描记录导出/导入同步携带（旧文件按 null 兼容导入）。
	修复元数据缓存并发覆盖冲突：MetaCacheRepository 覆盖刷新（先 DELETE 后 INSERT）加条纹锁按粒度串行化，解决导出表结构文档与扫描并发回源同表时的唯一键冲突（23505），补充并发回归单测。
	启动页新增自诊断面板：资源加载失败/脚本异常/服务长时间未就绪时自动展示，支持一键复制诊断信息；另在 docker-test-env 补充 Oracle 11g XE 测试环境与建库造数脚本。

================================================================

2026-09-01 周二
数据质检工具
	修复 SQL Server 2008 以下版本扫描整库报「找不到存储过程 sp_columns_100」：mssql-jdbc 的 getColumns 依赖该系统过程，2005 及以下没有；listColumns/countColumns 整表改写为 sys.columns/sys.types/sys.indexes 目录视图查询（2005+ 全版本可用），类型名到 JDBC 类型的映射在方言内自维护，并有单测覆盖。
	修复 Oracle 两类扫描报错：LONG/LONG RAW 列被当字符列做空串统计、被挑为分段键，而 Oracle 的 LONG 不能参与函数与比较（TRIM(LONG) 报 ORA-00932），归一为 Types.OTHER 只统计 NULL 数；DATE/TIMESTAMP 分段键的字符串字面量走会话 NLS 隐式转换报 ORA-01861，改为显式 TO_TIMESTAMP 固定格式，边界值输出统一为 yyyy-MM-dd HH:mm:ss.SSS，补日期键 seek/范围谓词生成 SQL 单测。
	运行日志（logs）从受控功能转为基础功能：任何实例恒可用，受控功能仅保留授权码管理；后端移除校验，前端侧边栏/路由守卫不再按 logs 过滤，签发工具提示与三篇 wiki 同步更新。
	新增 Oracle 11g 适配造数脚本 init-11g.sql：IDENTITY 列是 12c 特性，改用序列+触发器模拟；CONNECT BY 超过约 200 万行报 ORA-30009，orders/order_items 改走 CROSS JOIN 展开；大批量 INSERT 显式写 id 保证直接路径插入生效。

================================================================

2026-08-31 周一
数据质检工具
	抽样导出改为显式两步流程：上传 Excel 后先做数据源检测，任务停在「待导出」等用户决策，点「继续导出」才导出，支持重新导入全量替换明细并重跑检测；两步之间不保留内存解析行（含口令），导出时按 ds_key 重新解析数据源，ERROR 的用已存连接信息复测。
	检测明细实时三态（排队中/校验中/结果）展示，测连按完成先后翻行，单条 20s 超时；任务执行改 4 线程池，暂停中的任务不再阻塞后续任务。
	数据源编辑弹窗抽成独立组件 DatasourceEditDialog，数据源页与抽样导出明细就地编辑复用不跳页；同名数据源名称以导入表格为准（改名只动 name 列）；编辑保存不再做连通性探测，连接状态标记改为编辑时清零。

================================================================

2026-08-28 周五
数据质检工具
	新增瀚高 HighGo 数据库支持（HgdbJdbc 驱动+方言注册+前端图标），目标库扩至 8 种；KingbaseES 升级为多库方言，先选库再选 schema，与 SQL Server 口径一致，V23 迁移清空存量 schema 级过滤白名单。
	新增表格批量导入与抽样导出：上传每行一张表的 Excel，自动去重建档数据源（连不通标记 ERROR 待人工修复），每表抽 50 行按 数据源×类别 出 xlsx 并归档打 zip；V24 迁移新增任务/明细表与数据源连接状态列。
	扫描 AI 后续（打标/表描述）分类型计数，扫描列表进度条分段展示 AI 阶段；新增扫描任务手动结束接口，放弃挂起的 AI 后续直接落终态，解决进度卡 99% 的问题；新增最新扫描结果导出，取每表最近一次 DONE 快照，跨任务不依赖指定任务。

================================================================

2026-08-27 周四
数据质检工具
	新增系统诊断（排错中心）：聚合环境/授权/AI/数据源连通实测/失败记录/错误日志，支持导出 Markdown 诊断报告，用于现场排错。
	新增数据源整库表结构 Word 导出：导出数据源下所有白名单过滤后的库，走实时元数据（meta_cache 缓存优先，无需先扫描）；多库版 poi-tl 模板按 库→模式→表 两级克隆原型，目录条目渲染后由程序重建，页码由 Word/WPS 刷新域生成。
	修复 SQL Server 跨库元数据失效：连接池改为按库分池，借出连接时切好 catalog，规避 mssql-jdbc DatabaseMetaData 句柄跨库失效（错误 586/8179）。
	meta_table 新增 no_columns 无字段标记（V22 迁移）：扫描/字段访问发现无字段表置标记供跳过，刷新表结构自动还原；库过滤白名单支持系统库默认不勾选。

================================================================

2026-08-26 周三
数据质检工具
	AI 调用 Token/费用统计：新增 ai_usage_log 用量流水表（V16/V20）与 ai_config 计费价格列（V17），每次大模型调用成功即记录场景/模型/token/费用；计费支持峰谷计价开关（工作时间段高峰价、其余时间与周末谷价，默认 DeepSeek 官方价）；新增「模型用量统计」页，含汇总指标卡、金额/Token 双模式柱状图、场景分布与最近调用明细，明细改服务端分页（page/size，上限 100）。
	AI 用量迁入独立库并关联扫描任务：用量流水迁入独立 H2 库 dqaiusage（启动时一次性搬迁主库老数据），新增 scan_job_id 列（V21）关联扫描任务，统计页新增「按扫描」聚合维度；新增 ScanAiTracker 对扫描后的 AI 后续按 job 计数，全部表终态且 AI 清零任务才收尾 DONE/FAILED，此前保持 RUNNING、总进度封顶 99%，解决任务提前置 DONE 的问题。
	扫描记录跨机器导出/导入：任务连同事件/表/分段/字段明细打包 JSON 导出，另一台部署按数据源名预检映射导入，按 数据源+db+schema+created_at 幂等去重，单任务事务级联插入、失败回滚不中断整批；随后支持随任务携带表标记与表描述，导入后自动合并标注（按名幂等），本机无需重新打标，并有往返保真与重复导入幂等测试。
	扫描结果 Word 表结构文档导出：poi-tl 模板渲染扫描快照（封面+总体情况/库清单/表清单/逐表字段表），纯快照数据不回连业务库；模板由脚本把《水库矩阵平台数据库表结构文档_模板V1.0.docx》改造为标签模板。
	通用列表导出：表列表/库列表/字段明细/索引/数据预览/扫描记录等列表页新增「导出 Excel」，前端提交所见表格数据渲染 xlsx，一次性 token 下载，导出内容与页面过滤一致；统一下载封装 downloadFile，tauri 桌面版新增原生「另存为」命令（save_download_as，流式写盘，文件名以后端 Content-Disposition 为准）。
	数据源分组：data_source 新增 group_name 列（V18 迁移），卡片页按分组折叠渲染、支持分组筛选，侧边栏数据源菜单按分组成可折叠子菜单，分组随导入导出 JSON 与列表导出 Excel 一并迁移。
	桌面版体验：系统设置新增「应用模式首选浏览器」（自动/本机探测到的 Chromium 系浏览器，冷启动经镜像文件读取）；单实例接管升级，第二实例按前端指纹比对，同构建带出已有窗口，异构建（升级换包）按端口反查 PID 结束旧实例。
	构建修复：vite 启动全量预打包应用依赖，消除运行期新依赖发现导致的整页刷新；processResources 改为无条件 from web/dist 加可选输入声明，修复 release 构建同轮新建 dist 被 up-to-date 跳过导致静态资源拷不进 jar 的竞态；数据源卡片页移除导出 Excel 按钮（通用列表导出收口到其余列表页）。

================================================================

2026-08-25 周二
数据质检工具
	修复 CI 前端依赖安装失败：此前新增 codemirror 依赖只同步了 pnpm-lock.yaml，漏更新 package-lock.json，导致 release.yml 的 npm ci 报 EUSAGE 缺 16 个包；web 依赖安装整体改用 pnpm（pnpm/action-setup + frozen-lockfile），删除 npm lock，packageManager 固定 pnpm@11.7.0。
	tauri 依赖管理同步迁 pnpm，并恢复 windows-tauri NSIS 构建任务（此前因启动闪退停用，根因已修复）。
	新增 dq-tool-release 发布 skill：沉淀发布全流程（提交改动到 main、推送 GitHub、指定 tag 指向最新提交并 push，含同名分支/tag 的 refspec 与 force-with-lease 安全移动），供 AI 代理调用替代手工操作。

================================================================

2026-08-24 周一
数据质检工具
	扫描新增「生成表描述」开关（默认开，gen_doc 随任务持久化，V14 迁移）：表 DONE 后由 ScanDocService 独立线程池异步生成 AI 表说明，已有描述/未配大模型/任务终止自动跳过，LLM 失败按任务熔断。
	表标记增强：tag_def 增加描述字段（V15 迁移），AI 自动打标 prompt 携带标记描述辅助精准选择，打标弹窗与标记统计页支持描述编辑与展示。
	字段明细页新增「数据预览」页签：服务端真分页（每页 20 条可翻全表，COUNT(*) 实时总数），分页 SQL 走方言（LIMIT/OFFSET、SQL Server OFFSET/FETCH 或 ROW_NUMBER、Oracle OFFSET/FETCH 或 ROWNUM 包装）；支持 DataGrip 风格 WHERE/ORDER BY 过滤排序，输入框用 CodeMirror 6 精简封装，带 SQL 语法高亮与字段名/关键字智能提示。
	新增系统设置页与 system_settings 表（V13 迁移）：全局扫描默认值（并发/分段/阈值/超时）可视化覆盖与恢复默认；AI 配置表单抽为共享组件并新增「测试连接」最小请求探活。
	标记与描述导入支持数据源映射：新增 import/preview 预检接口解析文件里的数据源分布，导入时支持 文件数据源名→本机数据源 映射（同名自动预填，0=强制跳过），解决不同机器上同一数据源命名不同导致表级数据对不上的问题；配套不同名映射/映射跳过/预检聚合单测。
	前端构建收编进 Gradle（buildWebForRelease 作为 shadowJar 前置），make dev 不再构建前端，消除前后端构建耦合。

================================================================

2026-08-21 周五
数据质检工具
	启动优化：共享内核（H2 池/建表迁移/中断恢复）由后台线程与 Web 装配、绑定、开窗并行构建；新增就绪闸门与 /api/health 就绪探针，内核未就绪前业务接口统一 503 并带启动阶段，前端占位页轮询到就绪再挂载应用；安装版启用 JVM 原生 -splash 启动画面；新增启动耗时统计（各阶段打点，结束时输出表格汇总）；修复主线程与内核线程并发初始化 logback 拿到 SubstituteLoggerFactory 导致 ClassCastException 的问题。
	报告列表阶段文案改为换行显示在进度条下方，长文案不再把单元格撑宽。

================================================================

2026-08-20 周四
数据质检工具
	授权码按功能控制：新增 LicenseFeature 功能位（V12 迁移），签发/校验/记录支持按功能授权，侧边栏按授权过滤（运行日志需 logs、授权码管理需 admin+license_admin）；启动等待页在后端就绪探针通过前展示静态外壳，未就绪请求抛 ServiceNotReadyException。
	修复免安装版升级后白屏：index.html 与 SPA 回退响应发 Cache-Control: no-cache 每次重校验，指纹资源发 immutable 长缓存，防止升级后浏览器用旧入口引用已不存在的旧 hash 资源；带扩展名的静态文件缺失返回真实 404，不再回退成 html 导致模块脚本 MIME 报错；开窗前按前端 index.html 的 SHA-256 指纹校验浏览器缓存目录，不一致整体清理重建，无需用户手动删目录，并有四种场景单测。
	CI 发布流水线暂时只保留 Windows 免安装 zip（注释停用 windows-tauri 与 updater-manifest 任务）。
	界面改进：数据源列表页新增搜索框（名称/主机/用户名/类型）与收藏星标（本地偏好置顶），导出/导入按钮从下拉改为平铺；运行日志页异常堆栈独占一行，修复与消息并排挤压重叠；侧边栏顶部改为一行布局。

================================================================

2026-08-11 周二
数据质检工具
	侧边栏导航重构：新增左侧 200px 一级导航，「数据源」改为可展开树（点击直达库列表），页签栏仅保留下钻页；合并两个字段明细页为 TableDetail.vue（无扫描展示纯结构、有扫描叠加统计列与索引），路由统一并兼容回填旧链接的数据源上下文，刷新/直达 URL 也能恢复数据源真名。
	新增运行日志页：后端 LogStreamAppender 编程挂载到 root logger，环形缓冲最近 500 条并经 SSE 端点推送（先历史后实时）；前端终端风格展示，支持级别过滤/关键字搜索/自动滚动/清空；侧边栏支持收起/展开并持久化。
	扫描任务支持自定义并发 worker 线程数：ScanRequest 增加 workers 字段（V11 迁移），任务级覆盖全局默认，ScanExecutor 支持动态调整线程池大小，扫描详情页展示实际并发数。
	表结构浏览增强：未扫描表点击表名可看字段（名/类型/注释/约束）与索引明细；库/表/字段/索引结构本地缓存（meta_table/meta_column/meta_index，V10 迁移），首次访问拉取落 H2，浏览读缓存不连业务库，支持「刷新」强制拉最新结构。
	业务库执行 SQL 全量日志：SqlLogConnection JDK 连接代理，独立 logger 按天滚动，用于排查慢 SQL/深分页；前端面包屑导航替代页面标题+返回按钮，表列表支持按标记多选筛选。
	修复 tauri Windows 另存为必报「报告文件不存在或已被移动」：此前按 report-{id}.docx 猜文件名，与后端实际产物命名不一致，改由前端传后端返回的真实文件名定位。
	工程配套：版本号收敛为 VERSION 文件唯一源头并新增 bump-version.sh 一键发版（支持四段补丁版本映射 tag）；修复 package-win.bat 版本号解析含前导空格导致 CI 找不到 jar；补充水利行业标准文档。

================================================================

2026-08-10 周一
数据质检工具
	Word 数据调研报告导出：poi-tl 模板引擎渲染封面+四章正文（概览/库清单/表明细/字段统计），异步导出任务、后台生成，「导出任务」页查看进度与下载，tauri 原生「另存为」对话框；库描述在线编辑（V8 迁移）用于报告「实例描述」列；报告导出任务表（V9 迁移）启动时恢复未完成任务。
	修复 Navicat 未保存密码的连接无法导入：此前未存密码的连接直接计入失败，需手动改文件；改为与 JSON/DataGrip 导入一致照常导入，列表页标「未设密码」并 warning 提示补充。
	授权码本地调试：支持环境变量 DQ_LICENSE_PRIVATE_KEY_FILE 注入签发私钥，无需改 application.yml（避免打包产物因找不到私钥文件报错），makefile dev 自动注入。
	文档体系重构：AGENTS.md 从冗长文档改为索引，各领域细节下沉 docs/wiki/ 十个子文件；新增 JVM 桌面 UI 框架调研文档与 tauri 自动更新流程图。
	构建修复：package-win.bat 版本号改为从 build.gradle.kts 动态读取，修复 v1.7 CI 构建失败；CI updater-manifest 签名下载改 merge-multiple 平铺，修复只剩单平台时 latest.json 生成失败；导出任务列表增加搜索功能并调整列顺序。

================================================================

2026-08-08 周六
数据质检工具
	修复 tauri Windows 安装包启动闪退根因：Tauri 2 的 bundle.resources 落盘保留 resources/ 前缀，bundled_resources_dir 补查 <exe>/resources/，此前干净机器找不到内嵌 jar 直接 fatal 退出；同时补 windows_subsystem 配置，release exe 启动不再弹终端黑窗，拉起 java 子进程加 CREATE_NO_WINDOW。修复后恢复 windows-tauri NSIS 构建任务。
	桌面版极速启动：tauri 改常驻模型，窗口立即创建显示本地加载页，后台等后端就绪后跳转；关窗只隐藏不退出，托盘菜单「打开窗口/退出」，接入单实例插件与 macOS Dock 唤起；Flyway 增加快速路径，库已最新时跳过 migrate（实测省约 0.8s 启动耗时）；支持 JDK 25 AOT 类缓存。
	tauri 安装包自动更新：后台预下载+弹窗确认，updater 签名私钥入库；产物名统一为 dq-tool-<版本>-windows-x64-setup.exe。
	清理 Compose Desktop/JCEF 残留：Gradle 只剩 server/common 两模块，删除相关打包脚本、文档与 makefile 目标，桌面方案彻底收敛为 jpackage 安装版与 Tauri 两条路。
	Windows 打包脚本健壮性：jpackage 前清理旧 app-image 目录（不覆盖已存在目录，重复打包必失败）；bat 单行 if 块内中文在 GBK 代码页被 cmd 错位解析，改为多行块；经 MSYS2 的 make 调用时环境变量被剥光，脚本内对 TMP/JAVA_HOME 做兜底；密钥缺失/更新产物缺失时在打包步骤即报错，不再拖到后续步骤才失败；CI 暂时停用 macOS tauri 构建。

================================================================

2026-08-07 周五
数据质检工具
	架构重构：业务代码从 server/desktop 下沉为 common 共享内核（Kotlin），server 从 Spring Boot 迁移为 Javalin 薄壳；新增 JCEF 与 Tauri 两种桌面壳及配套打包脚本，CI 增加对应构建任务；同批落地表标记与统计、数据源 SSH 隧道、数据源导入导出（Navicat/DataGrip）、离线授权码、AI 表说明、明暗双主题。
	AI 自动打标：扫描可选开启（auto_tag 随 scan_job 持久化，V7 迁移），表 DONE 后 AutoTagService 独立线程池异步调大模型选 USER 标记打上，无注释上下文时抽样业务数据（前 20 列 100 行），同 job 首次 LLM 失败后熔断。
	授权码管理（仅管理员）：yml 配私钥文件即管理员实例，新增 /license-admin 页与接口，支持生成/留档（license_record 表，V6 迁移）/查看/删除；授权码 payload 扩展为 客户名|有效期|版本|server_url 等并兼容旧格式。
	数据库兼容性：SSH 隧道实现由 mwiede JSch 替换为 SSHJ；Oracle 表体积统计按 ALL_SEGMENTS → DBA_SEGMENTS → USER_SEGMENTS → 不统计 逐级降级（23ai 起 ALL_SEGMENTS 移除），探测结果内存缓存定时重探；老 SQL Server 兼容，启动时从 jdk.tls.disabledAlgorithms 摘掉 TLSv1/TLSv1.1。
	安装版排障能力：新增 StartupLog 早期日志（纯 JDK 实现不依赖 logback），从 main 第一行记录读配置/端口探测/内核装配/HTTP 监听每一步并接管各线程未捕获异常，解决闪退/卡死无日志可查的问题；新增 InstanceLock 单实例保护（双文件锁），第二实例带出已有窗口并退出，修复双实例经 H2 AUTO_SERVER 连旧实例跨版本类不兼容崩启动。
	日志与打包调整：日志目录改为数据目录同级 logs/ 按天滚动（保留 30 天）；内嵌 JRE 从 jlink 裁剪改为完整 JDK 复制后只删开发工具（实测达梦驱动初始化需要 jdk.charsets 的 EUC-KR，裁剪后运行时才炸）。
	其余修复：tauri Windows 编译失败（libc 信号处理限定 Unix 平台）；CI 打包三处问题（windows-tauri 缺前端依赖、pwsh 步骤静默吞批处理失败、npm 参数透传被当成 cargo 参数）；package-win.bat 安装包版本硬编码 1.5 导致 v1.6 产物错名，改为从 APP_VERSION 派生；前端耗时显示不足 1 秒显示毫秒；修复「首页」页签被污染后点击固定跳 '/' 的问题。

================================================================

2026-08-06 周四
数据质检工具
	项目从 Maven 单模块重构为 Gradle 多模块：拆分 server 与 desktop 子模块，共享依赖版本统一到 gradle/libs.versions.toml，打包脚本与 CI 工作流适配新结构，新增 Makefile 常用命令。
	桌面版生命周期：新增系统托盘常驻（打开窗口/退出），托盘生效时后端守护常驻、心跳看门狗停用；安装版启动即时反馈——托盘图标在 Spring 就绪前提前安装，新增 DesktopSplash 启动画面（图标+标题+进度条），就绪后关闭并打开 --app 窗口。
	修复托盘菜单两个 Windows 问题：中文显示方块（AWT 菜单 peer 对中文渲染有缺陷，全平台统一改 Swing JPopupMenu 由 Java2D 绘制）；高分屏缩放下菜单弹出位置偏移（不再取 TrayIcon 事件坐标，改取 MouseInfo 指针当前位置）。
	运行环境调整：内嵌容器 Tomcat 换成更轻量的 Jetty；pom 增加 orai18n 修复 Oracle 老字符集（ZHS16GBK 等）连接报 ORA-17056；默认端口 8080 改为 10000；全局异常处理与各层静默 catch 全部补上日志；授权码签发工具改交互式；桌面版迁移 Jewel 主题框架（自定义标题栏、深浅主题切换、捆绑字体修复中文渲染发虚）。

================================================================

2026-08-05 周三
数据质检工具
	授权码激活机制：离线 Ed25519 签名授权码（客户标识+有效期，支持永久授权），未激活或过期时所有 /api/** 返回 401、前端强制跳激活页；激活后授权码 AES-GCM 加密存 H2，重启免激活，到期日当天仍有效；首页底部展示授权信息并临期高亮（30 天橙色、7 天红色）；配套签发工具与 make license 快捷命令。新增桌面看门狗：--app 应用模式窗口心跳，窗口关闭超时自动退出进程。
	AI 表描述功能：新增 AI 配置管理（接口地址/API Key/模型），API Key AES-GCM 加密存 H2、回显只给 hasKey 不给明文；OpenAI 兼容大模型调用（RestClient 无新依赖），prompt 只含表结构元数据；表列表新增「描述」列，支持单表生成/刷新/手动编辑与勾选批量生成（前端并发 2，单表失败不影响其他）。
	库列表统计缓存：表数量/占用空间改走本地缓存（schema_stat 表），首次访问从业务库元数据拉取落库，之后只读缓存，创建扫描时按 schema 刷新，避免每次打开都连业务库；表列表页汇总条新增「字段数量」，方言层用 JDBC 元数据统计，7 种方言共用默认实现。
	表列表展示口径修正：优先展示最近一次扫描的准确行数/大小（非采样扫描完成的表显示 COUNT(*) 精确值不带「约」），顶部汇总与「只看空表」筛选同步改用有效值，修复元数据估算为 0 的表被误判为空表；修复从字段统计页/扫描记录页返回表列表时数据不刷新（keep-alive 下 onActivated 补充 load）。
	导出与启动体验：字段明细 sheet 每行前增加固定的「表名」「表注释」两列，便于脱离表列表单独阅读；安装版启动后优先探测 Chrome/Edge/Chromium 以 --app 应用模式开独立窗口（无地址栏），探测不到回落系统默认浏览器；Windows 安装包去掉控制台窗口，日志写入文件。
	工程升级：工具链升级到 Java 25 / Node 24（本地 35 个测试含 Testcontainers 全部通过）；CI actions 版本升级消除弃用告警；数据源空列表引导等界面细节优化。

================================================================

2026-08-04 周二
数据质检工具
	项目首次提交：Spring Boot 后端（数据源管理、库表浏览、并发分段扫描、断点续扫、Excel 导出）+ Vue 前端（Vite 构建），jpackage 打包脚本（Mac dmg / Windows exe，安装包启动后自动打开浏览器），GitHub Actions release workflow 推 v* tag 云端构建并挂 Release。
	CI 扩展为全平台构建：新增 macOS 双架构 dmg（arm64/x86_64）、Linux deb、Windows ARM64（Temurin 无 Windows ARM64 发行版，改用 Microsoft OpenJDK）；统一安装包命名为 dq-tool-<版本>-<系统>-<架构>；修复挂 Release 附件 403（workflow 补 contents: write 权限）与 macos-13 runner 退役导致 job 永远排队（x86_64 dmg 改用 macos-15-intel）；Windows 包改为 jpackage app-image 免安装 zip，不再依赖 WiX，解压即用。
	数据源保存/测试时自动探测数据库兼容模式：DbDialect 新增 detectDbMode 钩子，Kingbase 实现为执行 show database_mode 返回 pg/oracle/mysql；data_source 表新增 db_mode 列，新增/更新数据源时自动连库探测并保存，探测失败静默存 null 不阻塞保存。
