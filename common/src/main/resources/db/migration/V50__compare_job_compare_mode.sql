-- 数据比对:对比模式,新建向导第一步选择,区分「比多少字段」与「映射怎么来」两套口径。
-- compare_job.compare_mode:一任务一套模式,取值
--   ROW(行级对比,默认)   = 仅比对对象身份字段(对象编码/对象名称),确认对象存在性与身份一致性;
--                            字段少,字段映射由人工在画布连线完成(现有能力)
--   COLUMN(列级对比,新增) = 在身份对齐基础上逐字段比对全部信息字段(默认全选、可精简);
--                            字段映射由大模型预生成、人工在画布审核,避免人工连线数十张字段;
--                            全量扫描,数据量与耗时显著更高(重/小时级)
-- 执行引擎对两种模式同一套(fields 驱动 + mapping 驱动 + 全量分页扫描),模式只影响
-- 新建向导的默认字段选择与映射来源,以及列表/详情的模式标识;存量 NULL/缺列 = 行级,老任务兼容。
ALTER TABLE compare_job ADD COLUMN IF NOT EXISTS compare_mode VARCHAR(16) DEFAULT 'ROW';
