# 更新日志

## 2.0.18 (2026-09-22)

### 新增

- **比对导出支持「库/模式名反查字典」(V74)**:创建/编辑比对任务第 1 步可配一张字典表(可跨数据源,现有名称/真实库/真实模式三字段),导出 xlsx 表名单元格第三行「(库名.模式名)」按字典精准替换为真实名称出报告;查找键按 `库.模式` 合并串 → 模式单值 → 库单值依次精准匹配,未命中原样保留;只影响导出显示(字典读取失败静默降级照常出文件),比对执行与页面展示不反查,老任务 NULL 兼容

### 变更

- **批量导入库/schema/表名归一实际值**:schema 即 catalog 的方言(MySQL 系)忽略「模式名称」列(常误填 Oracle 风格别名);库/schema/表名再按元数据清单归一大小写与实际值(清单走缓存、读不到保持原值、不模糊猜测),单库方言 schema 槽位未命中回退用 db 槽位命中;接线到后台推导 phase 2、DS_ERROR 复活路径(confirmMapping)与向导提交统一校验,编辑向导反填同口径先校正再赋值
- **导出中心完整性标签措辞**:源文件校验和不匹配的标签由「已篡改」改为「已修改」(中性措辞,行为不变)

### 修复

- **Windows「打开目录/打开文件」静默失败开成「此电脑」**:`explorer /select` 按微软文档加内层引号;路径含 ASCII 逗号或超 250 字符时退化为打开所在目录(不选中文件),不再被 explorer 静默兜底到默认位置——绿色版解压到深目录/带空格路径的场景踩中率高(Tauri 兼容性台账 A10)

## 2.0.17 (2026-09-21)

### 变更

- **比对基准表显示名优先库描述**:比对任务列表「基准表」列第一行与差异明细页信息条改为「库描述(schema_doc,库列表页可编辑)> 数据源名」回落显示,数据源名只做兜底;比对导出 Excel 总览基准行同款口径(table_system 登记 > 库描述 > 数据源名),与目标侧显示名口径对齐

## 2.0.16 (2026-09-21)

### 新增

- **比对对象名称字段支持多选(V73)**:新建向导第 1 步「对象名称」由单选改多选,差异明细「对象」列取值 = 一行内按字段顺序取第一个非空(trim 后)的值;与身份字段互斥(前端勾一边自动摘另一边,后端交集直接 400);落 `compare_job.display_fields_json` 有序数组,旧列 `display_field` 继续写第一项,老任务与旧前端单值口径自动退化兼容
- **比对匹配引入「佐证字段」实体解析辅助证据**:行政区划/位置/所在河流三类文本字段(经纬度明确排除)参与配对判据——佐证一致加分、多属性独立冲突确定性拆开、单属性冲突交模型仲裁;缺失不算冲突、匹配只判同一性不判哪侧值对;字段按名称/注释规则自动识别,规则全未命中由大模型兜底挑(新增 AI 场景「比对佐证」),处置计数全部落目标说明披露——解决异地同名(如多地同名水库)被名称路错配的问题
- **比对 AI 判定逐条留痕(compare_ai_trace)**:比对阶段每次大模型调用(归一化补配/同名二轮消歧/批量导入映射推导/时间列识别/佐证字段识别)都落库留痕——完整 prompt、模型原始回答、逐条结构化判定结果、调用耗时毫秒;失败批次同样留痕;比对报告页工具条「AI 判定」与批量导入字段审核页「AI 推导依据」可按对比表逐条展开查看;随比对任务删除级联清理,留痕组装/写入任何失败只记 warn 绝不炸比对主流程
- **库列表「批量设置描述」**:从字典表(库名字段 + 描述字段)按库名精准匹配(trim 后完全相等)批量回写库描述,字典表可跨数据源选择;多库方言命中库对其下每个 schema 写同一描述;已命中旧描述覆盖、空行跳过不清空;完成后提示 已匹配/未匹配(附样例名)/跳过空行 统计

### 变更

- **比对取数主路径改流式单扫**:无 ORDER BY 无分页一次顺序全扫,DB 侧成本最低;流式开关按方言收敛(MySQL 系 fetchSize=Int.MIN_VALUE 逐行流,PG 系服务端游标);探测到服务端语句执行上限(PG statement_timeout/MySQL max_execution_time)或流式被服务端中止时自动降级分页 5000,降级原因透出到比对目标说明,现场不用翻日志
- **大模型补配提速减耗**:召回综合分低于 0.5 的候选不进模型;高置信(top1≥2.2 且与 top2 分差≥0.6)直接采纳不进模型;补配与同名消歧批次改 3 并发调用(按批次顺序合并,结果与串行同口径)并按批推进任务进度;同名消歧字段做区分度裁剪(剔身份/名称/全同值/UUID/序号列),字段标签用中文注释
- **通知系统改版**:通知移到右下角,标题+摘要固定两行折叠(完整内容去通知中心抽屉看);同内容通知旧条先关新条顶上、屏幕不叠加;屏上最多 5 条,超出最旧 FIFO 先关;未显式指定时长时按类型 success 2s/info 3s/warning 4s/error 5s
- **比对步骤日志**:统一「比对步骤」前缀的 info 级六步日志(任务配置解析→基准读取→目标读取→确定性对齐→大模型补配→逐字段比对与指标),照日志可复述整个比对过程;比对选表组件库/模式栏名称旁带库描述(过滤框同时匹配描述)

### 修复

- **比对佐证字段识别不计 AI 用量**:佐证兜底识别此前走未装配的默认 AiService 实例,现统一走共享实例,与其余场景一样落 ai_usage_log
- **字段映射画布身份徽章不刷新**:向导第 1 步渲染画布后反填/勾选身份字段、连线增删时就地重绘节点 HTML,「身份」徽章与基准字段加粗不再停留在旧状态
- **verifyChangelog 兼容 Gradle 10**:版本号改为配置期固化,消除执行期读 Task.project 的弃用报错

## 2.0.15 (2026-09-20)

### 新增

- **系统设置新增「页面心跳」调整项**:前端向后端上报心跳的间隔可在界面调整(单位秒/分/时,范围 1 秒~24 小时,默认 5 秒),保存后立即生效、无需刷新页面;桌面安装版的窗口看门狗超时随之自适应——按 max(配置超时, 3 个心跳间隔) 判定窗口已关闭,间隔调大后窗口开着不会被误杀(代价是关窗后后端退出更慢);间隔持久化在 system_settings(V71)
- **断网降级新增「本地沉淀推导」一级**:库清单或某库 schema 清单从未建立缓存且回源不可达时,不再直接报错——库清单从 `meta_table`/`schema_stat` 里出现过的库名推导(多库方言;单库方言取 `schema_stat` 的 schema 清单),schema 清单用库列表页沉淀的 `schema_stat` 概览缓存顶上;推导结果不回写 `meta_database`(只是本地见过的并集、未必全量),数据源恢复可达后刷新一次即重建真缓存。配套「默认库槽位兜底」:多库方言访问的库是 JDBC URL 里配置的默认库时,表清单/单表字段/schema 推导的读取键追加空串槽位,与扫描和库列表页 db='' 沉淀的缓存/快照同目录,断网推导降级出真实库名后比对选表器按库名取默认库的表/字段也能命中
- **比对/对象管理选表组件断网可用**:级联选表与对象管理选表组件在多库方言库清单为空时(断网且无缓存、或白名单滤空)自动降级为 db='' 单库链直拉 schema 清单,由后端本地缓存兜底,按三栏形态展示,与库列表页的 db='' 兜底同口径

### 变更

- **Tauri 安装版/绿色版保留开发者工具**:release 构建显式开启 devtools 特性,现场排查前端问题可直接右键「检查元素」

### 修复

- **make dev 本地开发偶发 `UnsupportedClassVersionError`**:Tauri 侧车的 java 解析(DQ_JAVA > 内嵌 JRE > PATH)不认 JAVA_HOME,PATH 里若有低版本 JDK 的 bin 会拉起低版本 java;makefile macOS 分支现显式导出 DQ_JAVA 指向 JDK 25
- **mac 打包脚本同步 versions 业务层布局**:配合三层分发改造,修正 mac 打包脚本的业务层目录拼装

## 2.0.14 (2026-09-18)

### 新增

- **系统设置新增「内存」调整项**:JVM 最大堆内存可在界面调整(512~8192MB,默认 1024),修改后重启应用生效——Tauri 桌面版由启动器在拉起后端前读取注入,jpackage 安装版/直接 java -jar 由后端启动时自检、与当前不一致自动按原参数重启一次;设置保存在数据目录 config.properties,页面同时显示当前进程实际生效值
- **比对身份字段下沉到目标级(V62)**:一个任务「一张基准表对多张业务表、各业务表身份字段不一致」原来撑不住——任务级身份由单选改多选(`key_fields_json`,语义降级为「默认身份/并集」),每张目标表的**有效身份 = 人工覆盖 ??「任务级身份 ∩ 该目标映射里已连线的基准字段」推导**(纯函数 `resolveIdentity`,提交/审核/执行/导出同一口径):只连上一个自动认它(多数场景零配置),连上多个默认组合身份(全部相等才算同一行),可在向导第 3 步或「字段审核」画布收缩为只用其中部分;一个都没连在提交/审核时 400 拦下并点名列出。行键为各身份列值 trim 后以 `\u0001` 拼接(单列即现状),身份字段恒入比对字段。老任务与批量导入 `key_fields_json` 为 NULL 时退化 `[keyField]`,口径与历史结果完全一致。字段映射画布同步把身份字段连线加粗、基准侧字段行加「身份」徽章
- **身份列为空的行不再被静默丢弃(V63)**:身份列(编码)为空的行原先直接跳过,整表空编码时目标表所有行都被当成「缺失」,导出总览只剩一片红。现在这类行以行内代理键进比对——编码路对它自然无命中,名称/大模型两路可把它配对(即「任意一边 code 空就用 name 匹配」),空行数落 `compare_target.no_key_rows`,并进 warn 日志、目标说明与导出「差异原因」(`身份列为空 n 行(仅按名称/大模型配对)`);`base_count`/`target_count` 口径统一为「比对时实际读到的总行数」,对象覆盖率如实反映跳过的行
- **比对任务列表服务端分页 + 批量删除**:任务多到一页拉不完,列表改为服务端分页(page 缺省 1、size 缺省 20 上限 500,返回 `{rows,total,page,size}`),首列加勾选列(row-key=id,1s 轮询重拉不丢勾选;RUNNING 不可勾,PENDING 静止态照常可删);工具栏新增「批量删除」走 `POST /api/compare-jobs/batch-delete`,逐任务同一删除口径、RUNNING 跳过不计入 `deleted` 并在提示里列出 T-id,操作列不再单设「删除」;操作列同时新增「导出比对报告」(仅 DONE,与差异明细页同一导出接口,直接下载 xlsx)
- **比对「同名对象二轮消歧」(可选,需大模型)**:名称首配存在歧义时(某名称双侧都有、至少一侧 ≥2 条同名,参与对象限未配对或按名称配对的行),把这些同名对象连同**全部比对字段取值**(经纬度/位置等)再交大模型重判哪些是同一名下的同一对象、哪些只是重名;有有效新配对的组整体重画(摘除组内首轮名称配对再落新配对,来源仍记名称路),未获配对的回落缺失/多余——解决「多个编码全空的同名水库按名称首配张冠李戴」。未配置大模型或参与对象超上限不执行,单组失败保持该组首轮配对
- **数据源不可达时新增第三级降级:扫描快照还原**:缓存也没命中时(该粒度从未浏览过、又 `refresh=true` 强制回源且连不上),从该 数据源+库+schema **最近一次 DONE 扫描的快照**(`scan_table`/`scan_column`)还原表清单/单表字段/schema 字段总数,还原后顺带回填 `meta_*` 缓存,下次访问直接命中、不再走降级;还原有限度——快照不含原始 typeName/jdbcType/索引明细(jdbcType 记 `Types.OTHER`,PK/UNI 从 `key_label` 还原),表不在快照中或表级非 DONE 不还原,回源恢复后首次刷新即被真实结构覆盖;快照也兜不住才原样抛 502/409

### 变更

- **比对导出总览「差异条数」改为纯数量差异,新增「匹配对象数」列**:差异条数 = 缺失对象数 + 多余对象数(原来还把「编码/名称不一致的对象」计进去,一列数字可能超过表行数);总览只做行级数量对比,属性差异(编码对不上、字段值不同)不计,「差异原因」仍写明 `编码不一致的对象 n 条` 供人工核对,字段级差异看「字段级差异汇总」与各目标明细 sheet。新增的「匹配对象数」= `matched_count`(编码/名称/大模型三路之和),「匹配编码数」列改按 `code_matched_count`(编码路命中)取值——客户核对表原只有编码一路,多路配对后总数单列不挤占旧列。「行级对比明细」sheet 同步新增「基准/业务表编码字段」「基准/业务表名称字段」四列(恒填字段名,与行取值无关),编码不一致的判定收紧为「两侧编码都非空且不同」(一侧编码为空属编码缺失,是字段级差异,不再计入,否则整表无码的目标会把所有配对都算成编码不一致)
- **元数据缓存写队列分「阻塞 / 火忘」两档,新增方言层读拦截器兜底回填**:`MetaWriteQueue` 原只有阻塞提交,扫描 worker 也要排队等写库。现在分两档——`submit` 阻塞等结果(浏览/导出等读路径,保持「提交后立即可读」与异常传播)、`submitAsync` 火忘提交(扫描路径,写失败只记 warn 不影响扫描),两档共用同一 FIFO,保证表清单覆盖先于随后的字段/索引覆盖落库。同时 `DialectFactory` 统一给方言套上 `MetaReadCachingDialect` 装饰器:任何路径经池化连接读到 库/schema/表/字段/索引/字段清单/DDL 且该粒度 `meta_*` **未缓存**时火忘回填(上下文 `MetaReadContext` 随 `SqlLogConnection` 连接代理携带;预览/测试连接等非池化连接无上下文纯透传;**已就绪粒度跳过**,避免 ChunkRunner 每分段 listColumns、数据预览等热路径写放大)——它是兜底回填,不替代各调用方语义各异的显式写(强制刷新、replace vs merge、no_columns 联动等),自身异常只记日志绝不影响元数据读取

### 修复

- **长时间使用后程序卡死、前端心跳接口报网络错误**:JVM 堆上限 384MB 在大表分段扫描、Excel/Word 导出与 AI 调用并发叠加时会被打满,进程陷入 GC 空转,所有接口超时,前端表现为「连不上接口」。堆上限统一上调至 1GB(gradle run、Makefile、jpackage/tauri 安装包所有启动入口),移除与 `-Xmx` 并存时本不生效的 `-XX:MaxRAMPercentage=50`;同时新增 OOM 兜底诊断——堆耗尽时自动把 heap dump 落到日志目录 `logs/oom-<pid>.hprof`,再发生可直接拿到现场分析
- **并发导入重复建任务 / 撞标记唯一键报主键冲突**:扫描记录导入的任务判重(同数据源+db+schema+创建时间)与标记按名合并都是「先查后写」,两个页签同时拉取局域网共享、或拉取与手工导入并行时,会双双通过判重产生重复扫描任务,并在 `tag_def.name` 唯一键上撞主键冲突导致整批导入失败。现在三个导入入口(`ScanTransferService`/`AnnotationTransferService`/`MetadataTransferService`)整体经进程内互斥锁 `TransferImportLock` 串行;导入与「标记管理页手工新建/改名」之间的并发由 `TagRepository.createIfAbsent`(撞唯一键读回既有标记)兜底,两侧都不会再裸报主键冲突
- **库列表页「批量扫描」提交的 schema 为 null 被后端拦下**:`ScanDialog` 的多目标契约是 `[{schema, database}]`,而库列表页行对象的库/架构名字段是 `name`,直接透传会把 `undefined` 序列化成 null 提交,后端按「schema 不得为 null」拒绝。现在库页显式映射 `rows.map(row => ({ schema: row.name, ... }))` 后再传,对话框内统一取 `schema ?? name` 并在缺失时提交前显式报错,不再发出无效请求
- **表清单缓存「就绪」但表行局部缺失时不再漏表**:表清单的就绪判断走 `meta_cache_flag` 标记,只代表「整粒度拉取过」,不代表行完好——`meta_table` 某行被删(或整粒度覆盖只落了部分表)时,浏览表列表会一直少这几张表。现在缓存命中路径会取该 数据源+库+schema 最近一次 DONE 扫描的快照,把**快照里有而缓存里没有的表** merge 补回 `meta_table`(只补缺失、不覆盖已有行、不动就绪标记)并并入返回结果(按表名排序),记 info 日志;这是缓存+快照的并集而非「拿旧缓存顶替回源」,故**不置 `X-Dq-Cache-Fallback` 降级标志**。快照数据可能滞后(源库已删的表仍会补回),`refresh=true` 回源成功后的整粒度覆盖会清掉滞后行

## 2.0.13 (2026-09-17)

### 修复

- **jpackage 免安装版导出/下载文件导不出来**:所有下载(导出当前列表、扫描结果/报告、比对报告等)原来在浏览器形态走 `window.open` 新开窗口,jpackage `--app` 应用窗口会把新窗口甩给系统默认浏览器(另一个浏览器配置),现场出现新窗口空白、文件拿不到。现统一改为在本窗口内 fetch 成 Blob 再触发浏览器下载,不再经过第二个浏览器;token 过期/服务端错误等异常响应也会直接弹出中文提示,而不是空白页
- **SQL Server 库列表页反复报「数据库 'dbo' 不存在」**:库清单未加载到时,概览/schema 清单回源把 schema 名(dbo 等)误写进库清单缓存槽位(`meta_database` 的 `db_name=''`),此后页面轮询把 dbo 当库名去切 catalog,每次访问刷一条错误。现在多库方言(SQL Server/Kingbase)database 为空时 schema 清单缓存键回落到解析后的默认库,并新增 V61 迁移清理存量污染(下次访问回源重建库清单);元数据导入同样按 V61 判定清理,修复前实例导出的文件不会把污染带回来
- **缓存子系统一致性排查加固(同源复核发现的 4 处隐患)**:① `schema_stat`/`scan_job` 的 db_name 空白值原样落 '' 会产生读取/删除口径(IS NULL)永远命不中的隐形行,现统一落 NULL;② Kingbase 显式库分池的 catalog 会覆盖「默认库」记录、让空 db 请求的目标库随建池顺序漂移,现只在默认池记录;③ 表说明生成与表结构 Word 导出两个接口不走统一 JSON 出口,缓存降级标志残留在 Jetty 线程上会串到下一个请求、误标「正在展示本地缓存」,现已消费清除;④ 另有两项确认为低风险暂维持现状(多库默认库的 ''/库名双槽位缓存、schema_stat 无「已缓存为空」标记,详见 docs/wiki/数据源.md)

## 2.0.12 (2026-09-17)

### 修复

- **跨版本升级/异常关机后 H2 库文件损坏直接无法启动**:启动报 `MVStoreException: File corrupted in chunk ...` 后进程退出,用户没有任何自救入口。现在打开连接池前先做健康预检(主库 + AI 用量库),异常链确认 MVStore 损坏才修复——损坏文件重命名为 `<库名>-corrupted-<时间戳>.mv.db` **永久备份不删除**,用 H2 自带 Recover 导出 SQL 再重建,尽量多救数据;导出/重建失败则以空库启动(Flyway 建表,数据可经导入功能恢复),保证程序一定起得来。预检/重建 URL 与正式启动同走 `AUTO_SERVER=TRUE`,自动接管上次强杀留下的残留锁文件;全过程落 `startup.log`(详见 docs/wiki/桌面版与数据目录.md)

## 2.0.11 (2026-09-17)

### 新增

- **比对任务批量导入(Excel 一 sheet 一任务)**:现场几十上百张表要比对、逐个走三步向导不现实——任务列表新增「导入表格」,上传一张 xlsx 批量建任务。一个 sheet 一个任务(按「是否基准表」列定位基准行,恰一行,不要求首行),表头 15 列按列名去空白定位、顺序不限;流程两步:①**数据源确认**(跨 sheet 汇总去重,按 数据库类型+IP+端口+库名 匹配已建档数据源,没有的用表格里的用户名/口令新建,两头都没有标异常)→ ②**建任务 + 大模型推导字段映射**,任务落「**待处理**」静止态(PENDING,不进执行器、不进后台任务中心);人在任务列表点「字段审核」在画布上核对/补线映射后开跑,或点「编辑」进向导逐步改(待处理编辑多一个「保存并比对」直接开跑),未配置大模型时提交直接 409 拦下。**导入的 Excel 原件按批次留档**(文件名不变、口令不落库),任务列表可见来源文件并可下载,另提供导入模版下载;新增 V59 迁移(compare_job 加 pending_reason/object_category/import_id + compare_import 批次表)、`CompareImportController` / `CompareImportExcelParser` / `CompareImportService` 与前端 `CompareMappingReview.vue`;比对相关接口与 `/api/compare-jobs` 同门禁(compare 菜单)
- **比对任务列表服务端筛选**:关键字(任务名/表名)、状态、标记(基准表或任一目标表命中)、数据源、匹配逻辑、对比模式,条件变化即重新拉取(与错误中心筛选条同模式)
- **元数据同步新增「指定库/schema」范围**(V58):同步范围由两档扩为三档——整个数据源 / 指定库/schema / 指定表,三种粒度可同批混合提交;同一数据源同时出现多种粒度时按 **整数据源 > schema 级 > 表级** 归一。前端多表选择器 `TableMultiPicker` 增加 `level='schema'` 粒度(级联终态栏为库/schema),元数据同步弹窗相应给出两个粒度入口
- **比对导出「数据最新更新时间」由占位改为真实快照**(V60):比对执行时探测时间字段——先按名称/注释命中 update 类(其次 create 类,同类型里日期时间类型优先),未命中则交大模型按字段语义挑「最新修改时间」字段(新增 AI 场景「比对时间」COMPARE_TIME),命中后对业务库取 MAX(列)落 `compare_job.base_data_updated_at` / `compare_target.data_updated_at`;表里没有可用时间字段、取数失败或未配置大模型一律留空。老任务(NULL)导出留空,历史结果不变
- **Oracle 字段/表注释**:连接属性显式打开 `remarksReporting=true`——ojdbc 默认不返回 REMARKS,注释此前恒空,现在随结构同步一起拿到

### 变更

- 数据源键计算与连接并发实测抽为共用件(`DatasourceKeyMatcher` / `ConnectionTester`),抽样导出与比对批量导入共用同一套「类型+地址+端口+库」口径与「4 线程 / 单条 20s 超时」实测(行为不变,去掉两份重复实现)
- 后台任务中心跟踪器按授权菜单裁剪:后端按菜单门禁的接口前缀(目前仅 compare)**在授权未开放时不发起轮询**,而不是打了 403 再静默跳过——关系推导后端不按菜单校验,保持照常轮询(细则见 ER 关系推导 · 异步任务与轮询)

### 修复

- **Tauri 安装版(Windows)所有 HTML5 拖放失效**:Tauri 默认把 WebView2 的拖放处理器换成只认文件的实现,非文件拖拽一律被拒——比对批量导入/抽样导出/数据源导入的拖拽上传、对象管理树内拖动、数据源卡片拖到分组在 Tauri 下全部收不到 drop(浏览器 / jpackage 形态正常,只测浏览器发现不了);现已在 Rust 侧关闭该处理器,详见 docs/wiki/Tauri兼容性.md
- **Tauri 安装版启动失败无任何提示**:release exe 是 GUI 子系统、没有控制台,`fatal()` 原先只写 stderr——内嵌 jar/JRE 缺失(如被杀软隔离)、端口探测失败、60 秒就绪超时,用户看到的都是「双击没反应」且无日志可查。现在失败原因会追加到数据目录同级的 `logs/tauri-startup.log`,Windows release 另弹一个原生错误框指明原因与日志路径
- **未开放 compare 菜单的实例每次启动产生一条 `API_403` 错误中心噪音**:前端错误上报与后台任务轮询统一尊重菜单授权——标了 `_silent` 的请求不再上报错误中心,受门禁的任务种类先查授权状态再决定是否轮询
- **恢复应用内自动更新清单**:`.github/workflows/release.yml` 的 `updater-manifest` 任务此前被注释,Release 上没有 `latest.json`,Tauri 安装版永远收不到更新提示;现已恢复(合并各平台 updater 签名生成清单,仅 tag 触发)

## 2.0.10 (2026-09-16)

### 新增

- **元数据批量同步支持「指定表」范围**:数据源页「刷新」弹窗新增同步范围单选——「整个数据源」(旧行为,勾选数据源列表)或「指定表」(只回源所选表);表级模式复用新抽出的多表选择器 `TableMultiPicker`(与比对任务「选择比对系统」同款交互:左侧级联逐表 +/−,右侧已选清单逐条可删),可跨数据源/库/schema 累积,表级模式的弹窗加宽到 1080px。提交走 `POST /api/metadata-sync`(body 新增 `tables: [{datasourceId,db,schema,table}]`,两种范围可只传其一或混合,同一数据源同批出现两种模式按整数据源处理)。任务明细新增 `tables_json`(V57,空 = 整数据源):表级模式只回源所选表——所在 schema 表清单整粒度覆盖(注释/估算行数/增删表对齐)、指定表逐张刷详细字段+索引并按批刷 lite 字段清单、重算 schema 字段总数;**库/schema 清单与库概览不动**(结构未变,留给整数据源同步或浏览时懒加载)

### 变更

- **比对报告总览「差异条数」改为对象级口径**:只统计 缺失 + 多余 + 编码/名称不一致 的对象数(按任务匹配逻辑的身份字段判定),不再把纯字段值不一致的对象计入——此前所有匹配对象只要任一字段不一致就算 1 条,差异条数可能超过表条数造成误读;新口径与「行级对比明细」sheet 完全一致(两者共用同一身份判定),「差异原因」同步新增「编码/名称不一致的对象 N 条」分量
- **数据比对差异导出缺失/多余对象逐字段展开**:各目标明细 sheet 中「基准有目标无/目标有基准无」的对象不再一对象一行留空字段列,而是按 `diff_json` 整行快照把**全部比对字段各出一行**——整行缺失的对象在该业务系统里所有字段都缺数据,基准侧字段名/中文/取值照常填、业务表值留空;多余对象反之(基准表值留空、业务侧照常填);缺失侧取值格与差异原因标红(此前「列级对比明细」已按字段展开,两张表口径从此一致)。同时「列级对比明细」sheet 由单行表头改为**两行表头**(第二行显示各侧表定位 `[库名][schema][表名]`,schema 为空省略该段),冻结范围由「左侧 5 列 + 首行」扩为**左侧 5 列 + 两行表头**(`createFreezePane(5,2)`,纵向滚动时表头不再跟着走)
- **比对向导第 2 步改用共用多表选择器**:「选择对比表」的「左侧级联 + 右侧已添加清单」两栏布局抽成独立组件 `TableMultiPicker`(左侧 `TableCascadePicker` 级联逐表 +/−,右侧清单逐条删除,加入前经 `validateAdd` 钩子拦截——比对任务据此拦下基准表本身;`lane-change` 回抛级联所在栏位供父级算置灰表),交互与口径不变;组件同时供元数据同步的「指定表」复用

### 修复

- **首装未激活实例卡在激活页、窗口反复消失又出现**:后台任务中心跟踪器在 App 挂载时无条件拉取推导/比对 active 接口,未激活实例这两个接口一律 401,axios 拦截器又按「授权失效跳激活页」把整页重定向到 `/activate`——地址没变但浏览器按整页重载处理,于是形成「激活页刚显示就重载」的死循环,用户连授权码都输不进去(仅在全新的、从未激活过的数据目录上出现,老用户升级因库里有授权码不受影响)。现在:`initBackgroundTasks` 首轮拉取前先读授权状态(复用路由守卫的状态缓存,同一次请求),未激活/已过期直接跳过;拦截器在已处于激活页时不再执行跳转(兜底,杜绝任何激活页请求再次触发重载);本会话内激活成功后由 `dq-license-changed` 补一次跟踪初始化,不必等整页刷新

## 2.0.9 (2026-09-16)

### 新增

- **错误中心(统一错误收集)**:新增一级页面 `/errors`(授权码**独立菜单 `error-center`**,签发对话框默认不勾选、可单独授权/屏蔽),把**前端 JS 运行时错误、未捕获 Promise、Vue 组件错误、`console.error`、接口 4xx/5xx 与网络错误**,以及**后端 ERROR/带异常的 WARN 日志、数据库 SQLException、未捕获线程异常、启动失败**全部统一采集进 H2 表 `error_record`(V56)。同一错误按指纹(来源+类型+归一化消息+首个业务栈帧)聚合为一条,显示累计次数与首次/最近发生时间,可按来源/级别/时间范围/关键词筛选、查看完整堆栈与上下文、导出 Markdown(发开发排错)或 JSON;默认保留 30 天、单表上限 2 万条,可手动按天数清理。**页面只读排错、不做错误处置**(无标记已处理/已忽略、无处理备注、无删除,仅保留「复制详情」与数据清理;`status`/`note` 列与对应接口随迁移保留但页面不再使用)
  - **授权口径双轨**:查看/统计/导出接口受 `error-center` 菜单控制(未开放 403),但 `POST /api/errors/report` **永远放行**——采集是基础设施,不能因为页面不可见就静默丢错误;未激活时查看接口 401、上报仍 200
  - 采集不阻塞业务:有界队列 + 单守护写线程异步落库;**内核建表/迁移未完成或写库失败时落 `logs/error-spool.jsonl`,就绪后自动回灌**,启动期与崩溃前的错误重启后仍可查;采集自身故障走独立 logger 且被采集器显式跳过,不会自噬递归
  - 前端上报按不可信输入处理(来源固定、字段截断、单批上限、限流),上下文不含访问令牌;同指纹 3s 去重 + 攒批上报,上报失败静默丢弃不递归;401(授权失效跳激活页)与 503(启动中就绪前)作为状态类响应不上报
  - 展示层**百分号解码**:路由/接口地址的查询串原文是编码过的(`?name=%E4%BB%BB%E5%8A%A1` → `?name=任务管理`),列表「位置」、详情「位置/上下文」、复制详情与导出 Markdown 均在展示时解码(只处理 `%XX`、不把 `+` 当空格、非法序列原样返回),数据库与 JSON 接口仍保留原始编码值,历史记录刷新即可读
  - 导出**可选行**:表格首列复选框(`row-key` + `reserve-selection`,跨分页保留勾选),勾选后导出菜单变为「导出所选 N 条 / 导出全部筛选结果」两组,所选按 `ids=` 精确导出(不受筛选与分页影响);操作列另有一键「导出」该行;改筛选条件自动清空勾选。后端 `GET /api/errors/export` 新增 `ids` 参数(非法 id 静默忽略)
- **数据比对差异明细新增「字段映射关系」只读连线图**:工具栏按钮弹出只读画布(复用 ER 画布 mapping 模式),按该任务各目标的 `基准字段 → 目标列` 映射画出连线,用于复核本次比对究竟按哪些字段对齐;老任务没有映射记录时按字段名忽略大小写自动匹配兜底展示并给出提示,不挂连线/删线事件
- **对象管理批量移动**:列表页勾选挂载表后点「设置目录」、挂载表的表详情面板勾选关系表后点「移动关系表」,复用「移动」弹窗选目标(目录/挂载表),前端逐条调单条移动接口,全部完成后汇总成功/失败数;移动后整树重拉并保持选中
- **对象管理目录图谱新增「ER 关系」显示开关**:目录图谱默认只画挂载/归属结构,工具栏勾选「ER 关系」后才拉取并叠加两端都在该目录表集合内的已确认 ER 推导边(不勾选时不发 `listRelations` 请求)
- 补齐被静默吞掉的异常:`ScanService` 的 `null_rules` 解析失败由静默改为 warn 落日志(会进错误中心),数据库版本号探测、SSH 隧道清理等预期内降级补 debug 说明

### 变更

- **元数据缓存写入统一串行,扫描只从原始库读结构**:结构缓存(meta_* 七张表 + schema_stat)新增全局单线程写队列 `MetaWriteQueue`,所有写操作排队串行执行,扫描多 worker 并发刷不同粒度不再互相等锁/撞唯一键(23505);`replaceTables` 整粒度覆盖表清单时顺带清理已删除表的字段/索引/整库字段清单/DDL/就绪标记。扫描(含续扫)口径明确为**只从原始库读、只往 H2 写**——绝不读本地结构缓存,每次扫描拿到最新表/字段/索引后整粒度覆盖(空字段表也覆盖为「空 + 就绪」并置无字段标记);浏览/导出/ER 推导等非扫描功能维持「缓存优先 + 回源覆盖 + 不可达降级」,AI 表说明生成改走 `MetadataService` 缓存优先路径(原先直连业务库绕过缓存)
- **数据比对新建向导由四步精简为三步**:「对比模式(列对比)」与「匹配逻辑」上移到第 1 步与选基准表同屏(原顶部 alert 引导条与「至少勾选 1 个比对字段」校验一并去掉);第 2 步只选对比表;字段表不再有勾选列——**参与比对的字段改由第 3 步的连线决定**(任一对比表连了线,连线基准端的字段即比对字段,提交时取全部连线的基准字段并集 + 身份两字段,对象编码/对象名称恒参与),「AI 预生成字段映射」也不再预圈字段(`fields` 留空 = 基准表全字段,由大模型按全表字段给建议);匹配逻辑界面不再提供「先编码后名称」(`CODE_THEN_NAME` 仅存量任务与直接调用兼容,界面收敛为 编码+名称 / 大模型归一化 两项,后者仍强制指定对象名称字段);字段映射画布**点连线不再直接删除**——先进入选中态(加粗变红)并在画布底部浮出确认条显示两端字段名,点「删除」才断线,连线增删改为按 id 差集就地增删(不整图重建、视口与节点位置不动)
- **数据比对差异明细展示口径调整**:删除单独的「基准信息」列(基准表定位串并入「对象」列头);业务系统列由「N 项不一致」改为三个小标签(一致 / 不一致 / 字段缺失,字段缺失为 0 时省略),「N 项不一致」不再把字段缺失计入;展开的字段级子表新增**蓝灰「字段缺失」格**(目标表无此列)与浅绿「一致」格,与不一致的红格区分;术语「列缺失」全文统一改称「字段缺失」(含 `diff_json` 里的 `«字段缺失»` 标记)
- **数据比对差异导出改版**:行级明细 sheet 末列新增「差异类型」(不一致/缺失/多余);字段级差异汇总新增「业务表字段」列且**只列与该业务表有连线的字段**;新增两张固定 sheet——第 4 张「数据级字段对比差异总览」(一行一条数据,按系统给 对比字段数/相同字段数/不同字段数 统计)与第 5 张「列级对比明细」(各系统逐字段取值横向合并成宽表,左侧 对象编码/名称/基准字段名/中文/基准表值 五列冻结,某系统未连线该字段则写「无此字段」);每个目标的明细 sheet 名由「序号_系统名」改为「序号_表名_数据源名」(同一系统多张表互比靠表名区分,表名因此放在 31 字符截断之前),并补业务表定位列(模式列按方言动态显隐)
- **扫描建任务改为异步**:`POST /api/scans` 只做数据源校验并落 PENDING 任务行(表数暂为 0)后立即返回 jobId,拉表清单(必连业务库)、结构缓存同步、`scan_table` 落库移到后台引导任务——大 schema 或半死库不再把接口卡几十秒(实测 Oracle 未设超时时建连可耗到 84s,期间前端早已超时报错、后端还在跑);代价是「选中的表在库中不存在」「没有不超过大小上限的表」「该库下没有表」这类校验由同步 HTTP 4xx 变为任务落 FAILED + `error` 文案(详情页「错误信息」展示);另外带表名清单的扫描走局部缓存合并、不再顺带刷新库列表统计
- **业务库连接新增建连/读取超时**:新增配置 `dq.scan.db-connect-timeout-seconds`(默认 15s)与 `dq.scan.db-read-timeout-seconds`(默认 1800s,<=0 不限制),由各方言 `connectionTimeoutProperties` 映射成驱动属性(MySQL / PostgreSQL 系含金仓瀚高 / SQL Server / Oracle),建池与一次性试连(连接测试、维护库探测)两处统一透传;达梦未覆盖走驱动默认。注意读超时默认 30 分钟即中断,慢库长任务会因此失败(可在配置文件调大或设 0)
- **只扫少量表时按表名过滤字典查询**:`DbDialect.listTables` 支持把 `AND 表名 IN (...)` 拼进原字典 SQL(上限 500 张,超出退回整库拉取后内存过滤),Oracle/达梦这种「表清单 SQL 每行都要做体积子查询」的库不再为扫一张表付整库字典成本
- **Oracle 表体积 SQL 改为分组聚合 + LEFT JOIN**:`all_segments` 与索引段各按 owner+表名聚合一次,再与 `all_tables` 左连相加,替代原先对每张表各跑一遍段视图的相关子查询(三条降级链一并改写);语义保持「表段与索引段都不可见时体积为未知」
- **扫描详情「单表扫描」入口移到表格操作列**:由工具栏常驻按钮(下拉选表)改为每行固定「操作」列的「扫描」按钮,点击即以该行表为唯一范围打开共享扫描对话框(范围项显示「仅扫描表:xxx」,默认强制全量开、大小上限留空);`ScanDialog` 相应删除表清单下拉模式
- **更新日志页改为按 Markdown 渲染**:后端 `/api/changelog` 条目正文由「已 trim 的文本行数组」(`lines`)改为**原始 Markdown 字符串**(`markdown`,保留段内空行与行首缩进),前端改用新增依赖 `marked` 渲染,标题/列表/表格/引用/行内代码等按 Markdown 语义展示(CHANGELOG.md 是构建期打包进 jar 的可信静态内容,故直接 `v-html` 输出)
- **授权码菜单口径调整**:签发对话框默认勾选**除 数据比对 / 系统诊断 / 错误中心 / 授权管理 外**的全部菜单(受控与排错向功能默认关闭以免误下发,仍可手动勾选),菜单清单同步新增「错误中心」;旧格式授权码(无菜单段)按旧功能段推论,自动开放含错误中心在内的全部菜单(除 数据比对/授权管理)。**注意**:已签发的带菜单段授权码里没有 `error-center`,升级后错误中心页面会 403,需重新签发并勾选该菜单(采集不受菜单影响,上报接口永远放行)

### 修复

- 运行日志(SSE)堆栈每行出现重复的 `at at ...` 前缀:logback 的 `StackTraceElementProxy.toString()` 自带 `at `,拼接时不再重复补
- 静态资源 Content-Type 表由 8 种扩展到 40+ 种(文本类补 `charset=utf-8`,新增 `wasm`、`woff`、音视频与数据类等,`bin/exe/dll` 显式 octet-stream,无扩展名请求不再越界):此前字体/wasm 等一律按 `application/octet-stream` 下发

## 2.0.8 (2026-09-15)

### 新增

- **AI 自动打标已有标签表处理模式**:扫描任务新增 `auto_tag_mode`(V54),可选 跳过 `SKIP`(默认,保持升级前行为)/增量追加 `APPEND`/全量覆盖 `OVERWRITE`;共享扫描对话框勾选 AI 自动打标后出现三选一,覆盖模式只替换 AI 来源旧标记,人工与系统标记保留
- **备份表系统标记**:表名以 `_copy`/`_bak`/`_backup`/`_tmp` + 可选序号结尾的备份/临时表,扫描后由系统自动打/摘「备份表」标记(V55,系统标记不可人工编辑);AI 自动打标对这类表直接跳过并清理陈旧 AI 标记,Word 报告数据冗余分析复用同一判定
- **任务级采样行数与共享扫描对话框**:扫描请求支持 `sampleRows` 任务级覆盖全局默认(V53);表列表/库列表/扫描详情统一复用 `ScanDialog.vue`,扫描详情新增「单表扫描」下拉重扫,批量扫描改为逐库提交后汇总提示、不跳转详情页
- **对象管理移动与目录树增强**:目录树常驻关键字搜索与展开/收起所有;目录、挂载表、关系表均支持树内拖动或「移动」弹窗变更所属,后端新增 move 接口并校验防环、跨数据源、同级重名与重复登记
- **授权码留档备注**:签发授权码可填写备注,留档表新增 `remark` 列(V52),仅管理端展示,不写入授权码 payload
- **Word 报告部分导出**:库列表选中的库中,有 DONE 快照的照常导出;无快照或覆盖不全的库原因写入报告新增「1.3 导出说明」节,只有所选库全部无 DONE 快照时才任务失败

### 变更

- 关系推导整库字段清单一次拉取失败时降级逐表拉取(兼容 SQL Server 2005- 缺 `sp_columns_100` 等场景),单表失败跳过不中断任务,逐表全败才判失败
- 后台任务中心增加常驻入口与角标计数,无活动任务时也可打开任务抽屉查看
- 扫描详情表名链接全部状态可点,未完成行按结构元数据兜底渲染;失败原因支持点击查看完整错误
- 数据比对差异导出升级为字段级九列口径,并新增「行级对比明细」「字段级差异汇总」固定 sheet(本项同步补记 2.0.7 更新日志)
- 标记统计与打标弹窗对系统标记(空表、备份表)统一只读,不参与批量打标及标注导入导出

## 2.0.7 (2026-09-14)

### 新增

- **授权码菜单级控制与免接口鉴权标记**:签发授权码不再区分基础/受控功能,改为**菜单列表勾选即展示**——16 个一级菜单全量可选(带全选/半选),客户实例侧边栏只显示勾选的菜单,未勾选菜单的路由守卫拦截、相关接口 403;授权码 payload 升级为 10 段格式(新增菜单段与免鉴权标记段,旧格式授权码兼容,按旧功能段推导菜单、行为与旧版一致);新增「**免鉴权**」标记(演示用):勾选后该实例已激活未过期期间所有请求跳过 `dq.access-token` 访问令牌校验。签发对话框加宽平铺全部菜单;留档表新增 menus/bypass_auth 列(V51)
- **数据比对列级对比模式**:新建向导可选「行级+列级」对比(默认行级)——身份对齐后逐字段比对全部信息字段(向导默认全选),字段映射由**大模型一键预生成**(新接口 `POST /api/compare-jobs/mapping-suggest`:基准需映射字段+目标全列发给模型,产出「基准字段→目标列」建议,画布人工审核增删后再提交;未配置大模型给出引导),落库 `compare_job.compare_mode`(V50,老任务空=行级、历史结果口径不变),任务列表/差异明细展示模式标识;匹配逻辑选项简化为「编码+名称」与「先编码后名称+大模型归一化」两档(旧档存量任务与 API 仍兼容)
- **后台任务中心**:关系推导与数据比对两类长任务后台异步化——头栏新增任务指示器(单任务显示 种类·阶段·百分比,多任务显示计数,hover 逐项进度),点击打开任务抽屉(按种类分区,行=定位串+阶段中文+进度条+步数+耗时,点击行直达结果现场:推导→表字段明细 ER 页签、比对→差异明细);任务到终态弹**可点击直达结果页**的通知并留痕通知中心,页面已自行渲染终态的自动去重不重复弹;离开页面不再丢任务进度。配套新增 1s 轮询接口 `GET /api/relation-infer-jobs/active`、`GET /api/compare-jobs/active`;推导任务新增「排队中」(PENDING)态——提交即入库、开始执行才计时(重启残留 PENDING/RUNNING 统一置失败)
- **画布「另存为图片」工具**:所有画布(ER 图/字段映射等)工具栏常驻第 6 图标位,整张图(不止视口)合成 PNG 下载——HTML 节点按世界坐标栅格化叠加,超大图按 8192px 收敛,底衬随亮/暗主题;导出文件名带图名(如「ER图」)
- **SQL 控制台表清单面板**:编辑器右侧列出当前库全部表(英文名+注释),与智能提示同一份缓存数据联动刷新、渐进补齐
- **关系批量处理增强**:新增「仅重复连线」筛选(同一对表间存在多条连线)+「选中多余连线」按钮——按表对分组、每组保留一条(已确认优先)、其余选中,配合批量否决一键清理重复关系;勾选态 reserve-selection(任何筛选不丢勾选,被筛掉的已选红色提醒并可置顶展示);点击行任意位置切换勾选,hover 副表时列表中同名表绿色高亮,列表按副表名排序聚拢同名表
- 数据比对字段映射画布增强:连线**按目标表着色**(8 色调色板、同表恒同色,多目标一眼分清连线归属);**悬停连线反馈**(该线加粗+两端字段行高亮+跟随鼠标浮层显示两端字段名,命中宽度放宽 10px);mapping 模式支持**全屏**;字段行 0.5px 发丝分隔线;工具条新增**「映射管理」弹窗**(全量映射清单,按对比系统/关键字筛选,单行与批量删除即断线、随时可重连);节点标题统一「中文表名加粗第一行+英文定位串第二行」;移除基准字段常亮底色
- 对象管理挂载表/添加关系表弹窗加宽(960→1080 / 720→920)并抬高选表清单高度,描述列更宽;选表清单过滤/刷新不丢已选,已选按勾选顺序置顶

### 变更

- 数据比对差异导出明细 sheet 改为**字段级九列口径**(对齐客户核对表):首行即表头(无上下文/图例行),前两列对象编码/名称,基准表块(基准表字段/字段中文/基准表值)与业务表块(业务表字段名/业务表中文/业务表值)各三列,末列差异原因;DIFF 行由「一格一个对象」改为**逐不一致字段一行**(红底标基准/业务两个取值格,目标缺列写「目标无此列」),MISSING/EXTRA 仍一对象一行整行标红;业务表字段名按任务字段映射还原目标列名;新增固定第二 sheet「**行级对比明细**」:一行一个「对象 × 比对目标」,基准/业务两侧各带表英文名/表中文名/编码/名称,末列差异说明;新增固定第三 sheet「**字段级差异汇总**」:一行一个「比对目标 × 基准字段」,差异数量按 缺失/多余/不一致 三类拆分;移除「对象差异汇总」分组 sheet
- 桌面版心跳看门狗**休眠挂起容忍**:每轮同时对比墙钟与单调钟增量,差值超 10s 判定进程曾被挂起(系统休眠),本轮跳过退出判定并把心跳基线重置到唤醒时刻——笔记本合盖唤醒后不再误退出;窗口真关则唤醒后超时照常退出(时钟可注入,新增单测覆盖挂起/真关/托盘/无心跳路径)
- 数据源编辑对话框「加载」库列表改为**强制实时回源**;源库不可达降级读本地缓存时弹警告,明确告知「当前展示的是本地缓存库列表,可能与源库不一致」
- 推导弹窗后台化:关闭弹窗任务不停(全局跟踪器接管进度与通知);重开弹窗时同一(数据源/库/schema/锚点表)有活动任务直接进入运行阶段恢复现场;进度区新增「排队中」态与提示
- 比对新建向导交互:步骤条可点击已激活步骤回看(纯切视图,未激活灰显禁点);数据链联动——前序步骤数据被改即清空后续所有步骤并收回激活进度;回看第 1 步未换表不再重复加载字段;「对象名称」列未勾选字段也能选(选中自动勾上);提交后登记后台任务跟踪,离开页面也能收到完成通知
- 通知组件支持自定义标题与点击回调(后台任务完成通知靠它直达结果页)
- 补全 2.0.6 更新日志:字段映射画布「连线按对比表着色、悬停浮层、映射管理弹窗、节点标题两行化」等修复条目

## 2.0.6 (2026-09-14)

### 变更

- 交付形态调整:fat jar 变为**纯 API 服务**(`shadowJar` 排除 `static/**`,不再内嵌前端资源),Tauri 桌面端从本地 `web/dist` 直载页面(秒开,不再 navigate),API 走 `127.0.0.1` 动态端口;前端资源不再需要反复打开 75MB fat jar,缓解 Windows 杀软实时扫描导致的 assets 404
- jpackage 分发线(Windows 免安装 zip / macOS dmg / Linux)保留页面能力:打包脚本把 `web/dist` 放到应用镜像 `static/`,运行时经新增的 `-Ddq.web.static-dir` 从磁盘发前端

### 安全

- 新增浏览器访问管控 `dq.access-token`:配置后浏览器直接访问后端端口须携带令牌(请求头 `X-Dq-Token` / `?token=` query / Cookie 三选一命中),仅 `/api/health`、`/api/license/status`、`/api/lan/share/**` 免令牌;Tauri 每次启动随机生成并经 `-D` 注入,用户无感;令牌值不落日志、不回传接口
- 后端接入 CORS(`/api/*` 任意来源),仅用于 Tauri webview 跨域访问本机 API,配合上面的令牌门禁;`allowCredentials=false`

## 2.0.5 (2026-09-13)

### 新增

- 数据比对新增**对象匹配逻辑**(三方数据比对最容易踩的坑:对比表与基准表的身份字段对不上,同一条数据「code 一致 name 不一致」或「name 相同 code 不同」):新建向导第 2 步可选择三种判定「是否同一个对象」的口径 —— ① 编码+名称都相等(默认,最严格);② 先编码后名称(有编码先用编码配,编码没配上的对象再用对象名称配一轮);③ 编码/名称+大模型归一化(1、2 都没配上的残余对象交大模型按业务含义再认一轮,如简称/别名/错别字)。匹配逻辑 2/3 使用「对象编码」(比对主键)与「对象名称」两个字段,故选择后必须指定对象名称字段(前端拦下、后端 400);匹配逻辑 3 在未配置大模型、双侧残余超过 2000 条或模型调用失败时**自动降级**(残余按缺失/多余处理,原因写进目标说明),不会让整个任务失败。落库新增 `compare_job.match_mode`(V49,老任务为空 = 仅按编码对齐,历史结果口径不变)、`compare_target.code/name/ai_matched_count`(按来源统计命中数)与 `compare_diff.match_by`(CODE/NAME/LLM);差异明细里靠名称或大模型配上的对象打「名称配对 / AI 配对」标签,质量报告新增「对象命中」列(悬停展开编码/名称/大模型各自命中数),大模型调用按新场景「比对匹配」计入 AI 用量统计
- ER 关系人工审核留痕(重构要点):新增「原始关系」快照表 `table_relation_original`(V47 迁移),原始关系 = 大模型推导产出的关系、每次推导刷新而人工审核不改变它;最终关系 = 人工审核最后一次修改后的结果(`table_relation`,新增 `reviewed`/`reviewed_at` 标记人工已审核)。重新推导时人工已确认/已否决的关系**一律保留上次审核结论**(不再回炉为候选),只有从未审核的新关系才留给人工审核;迁移按「当前推导属性 + 状态 CANDIDATE」回填存量关系的原始快照,MANUAL 关系算人工新增
- 「导出ER关系 / 导出ER表格」改为导出 3 个 sheet,顺序固定为**表清单 / 推导关系清单 / 关系变化**,三者**同表头同数据逻辑**(库名/模式/表英文名称/表中文名称/数据量/级联字段编码/级联字段名称/标签;每表 × 该表作为关系端点的级联字段;有中心/锚点表时整表不列即不显示主表只显示副表):表清单取当前图、推导关系清单取大模型推导产出的 `table_relation_original`;关系变化**逐「表.字段」做差、只保留只在一侧出现的行**(两边都有的 表.字段 不算变化,不因同表另有字段差异而被带出),并在末列追加「备注」= 人工审核时填写的备注说明(人工否决原因即在此体现)

### 变更

- 关系批量处理对话框:「备注」列由只读文本改为**输入框**(人工填写否决原因,随本次「批量确认/批量否决」一并落库,只提交本次真正改过的行、未动的行保留原备注);移除「批量删除」按钮,只保留批量确认与批量否决两个按钮(逐条删除仍在关系详情抽屉,接口 `DELETE /api/relations/{id}` 与 `POST /api/relations/batch-delete` 保留)

- 数据比对的「导出差异表」改为按客户既有核对表格式导出「比对报告」(文件名 `比对总览-{任务id}.xlsx`,按钮文案同步改为「导出比对报告」):首个 sheet 固定为「总览」,列口径为 表中文名(表注释)/表英文名称/所属系统(table_system 登记值,未登记回落数据源名)/条数(该侧表实际行数,目标侧含多余行)/数据最新更新时间(当前未采集,固定「—」占位)/与基准差(目标行数 − 基准行数)/匹配编码数(SAME+DIFF)/差异条数(缺失+多余+不一致对象)/差异原因(按差异构成自动拼写,可导出后人工补充),第一行为基准表本身,其后一行一个比对目标
- 明细 sheet 采用精简版(对齐客户核对表):第 1 行单行上下文、第 3 行表头(字段注释做中文列名 + 末列「说明」)、其后一行一个差异对象且一格一个对象;为此 `compare_diff.diff_json` 改为整行快照(DIFF 行每个比对字段各一条、value 仅不一致字段非空,MISSING/EXTRA 单侧齐备),前端展示与问题字段排行口径不变,老任务仍可正常展示
- 总览之后**总览里有几条差异数据就跟着几个明细 sheet**(即一个比对系统一张,sheet 名 `序号_数据源名`,序号与总览行一一对应):每个 sheet 装该系统**全部**差异,粒度为字段级——说明列写清差异(DIFF 行形如 `字段: 基准值 → 目标值`,缺失/多余写「基准有目标无」「目标有基准无」);明细行数超 100 万行(Excel 单表上限兜底)时截断并标注
- 比对目标新增实际总行数落库(V44 迁移 `compare_target.target_count`),供总览「条数/与基准差」使用;历史任务无该值时用「匹配数 + 多余数」兜底
- 侧边栏数据源的编辑图标改为就地弹编辑框:在任意页面点该图标都直接弹出当前页的数据源编辑框,不再先跳到数据源页再弹(弹框组件移到 App.vue 持有,侧边栏行与数据源页卡片同源);保存后广播的列表变更会让侧边栏与当前数据源页卡片一起刷新
- 库过滤口径前后端统一:数据源未配置库白名单时,接口按「默认规则」只返回业务库(排除各方言系统库),与「库过滤」页签里系统库默认不勾选完全一致 —— 之前是「未配置 = 不过滤 = 系统库照样返回」,页面上系统库没勾选、查询库列表却又把系统库带回来;需要在库列表/扫描/选库里包含系统库时,在库过滤页签勾上并保存(写入显式白名单)即可。「全勾/全不勾 = 不过滤」的提交口径同步收敛到前端 `utils/schemaFilter.js`,避免「全勾(含系统库)」被默认规则吃掉
- ER 图「关系管理」对话框补齐表的筛选:新增按表标记多选筛选(关系的两端表任一命中即展示,下拉只列当前清单里实际用到的标记)与表格「标记」列(两端标记合并去重,空表标记浅色区分);原「按表名过滤」升级为关键字搜索,一次命中两端表名、表注释、两端字段名与备注;三项过滤仍在纯前端完成,切换不回源,`GET /api/relations` 接口保持不变
- ER 图「关系管理」对话框列结构调整:列名改为「主表 / 副表」,**中心表恒排在主表列**(它在库里可能本身处于 many 侧,旧实现直接照搬 one/many 顺序会把它排到第二列),两列统一为「第一行中文表名 + 第二行 `表名 [字段名]`」(不再用点号连接),取消单独的「多端字段」列;主表列行尾右对齐、副表列行首左对齐标注重数 `1`/`N`,按存储方向还原(中心表在重复侧时主表标 N、副表标 1,1:1 与疑似多对多两端均为 `1`),两端标记与表标记同款圆角标签但 `N`(多端)用橙色、`1`(唯一端)用主题蓝,两列各自显示本端真实重数(展示行的基数简称 `card` 由接口的 `cardinality` 归一而来,1:N 关系中心表在重复侧时主表标 N、副表标 1,1:1 两端均为 1),基数由两端标记表达,故「关系/基数」列整列删除
- ER 图「关系管理」对话框清单范围与默认口径调整:表详情页 ER 关系页签入口改为**接口只拉本表参与的关系**(`GET /api/relations?table=` 两端任一命中),不再拉整库再前端过滤——清空弹窗上全部筛选条件后,列表呈现的仍是该中心表的完整相关关系;状态筛选默认由「仅候选」改为「全部」,星型图入口也不再预填关键字(状态切「仅候选」、输关键字仍是可选收窄手段);ER 关系页全库总图入口保持整库清单

### 修复

- 修复数据比对向导第 4 步「字段映射」画布的一串问题:**连线渲染错位**(mapping 模式端点计算把节点中心又加了一次半宽半高,线全部画到卡片右下方、看起来像没连线——已按 getCenter() 统一口径修掉);**点击连线经常没反应**(create-edge 临时边 id 与已存在的正式映射边撞 id,G6 抛 Edge already exists 中断交互、橡皮筋残留,已改唯一临时 id、由父级按映射重建收敛);**鼠标悬停很卡**(hover 高亮原来走整图 setData+draw 重绘,改为直接改行 DOM,换行才动一次手);**初始视口内容飞出屏幕**(fitCenter 聚焦的是画布包围盒而非图内容,改为按节点并集包围盒直接落相机位姿);字段映射画布**字段全量展开**(不再「+N 个字段」折叠,目标行恒可点);进入画布时**不再自动按名称连线**(画布空白由用户人工连线,「按名称自动匹配」按钮一键填充,口径不变);连线**按对比表着色**、悬停连线浮层显示两端字段名并加粗高亮(人工审核与点选体验);字段行上下细分隔线改 0.5px 发丝渐变;「AI 预生成字段映射」按钮挪到工具条最左(按名称自动匹配左侧);新增「映射管理」弹窗(参考 ER 图关系管理:全量映射清单、按对比系统/关键字筛选、单行与批量删除,删后画布断线可重连;首列=数据源名+中文表名/换行英文定位串);画布表节点标题统一「中文表名加粗第一行、英文定位串第二行」(表注释按 schema 分组从表清单接口取);顺带修复画布底座 fit='center' 的居中口径(表详情 ER 页签同样受益)与鸟瞰图对 create-edge 辅助节点的 cloneNode 报错
- 修复数据源编辑对话框「库过滤 → 加载库列表」对 MySQL 等单库方言提示「目标库没有可选择的库」、已选 0/0 的问题:回源取库清单时只调了 `listDatabases`(单库方言未实现、恒为空),空值又把本地库清单缓存连同同一批 schema 行一起覆盖成空;现按方言统一口径 `listDatabasesOrSchemas`(单库方言回落 schema 列表),缓存回源与「按表单连接参数实时拉取」两条路径共用同一实现
- 修复头栏「刷新」(软刷新)后侧边栏数据源 ⋮ 菜单的「新增/导入/导出」点击没反应:软刷新换 keep-alive key 后,旧的数据源页实例仍留在缓存里,它的 watcher 抢先消费了侧边栏写入的待办标记,弹窗开在已停用的隐藏子树上,当前可见实例什么都不显示;现改为停用实例不消费标记、页面激活时补一次消费

## 2.0.4 (2026-09-13)

### 新增

- 数据比对(受控功能,授权码需显式包含 compare):选定一张基准表,与多个目标系统的对应表按比对主键逐字段比对,产出差异明细(完全一致/字段不一致/对象缺失/对象多余四类,字段级逐字段展开)与质量报告(对象覆盖率、字段一致率、数据完整率、综合评分四比率 + 问题字段排行);差异明细支持按类型/厂商/关键字筛选并导出 Excel;后台执行带阶段进度,单目标失败不中断整批,支持重新比对、归档、删除;数值字段自动归一(千分位/小数尾零),NULL 与空串视为一致,单侧上限 50 万行
- 数据源页新增「导出元数据」:把数据源的元数据缓存(库/表/字段/索引/DDL 等结构缓存)连同派生标注数据(表说明、表标记、ER 关系、数据目录等)打包成 JSON 文件;现场人员「刷新」元数据后导出,交给连不上业务库的人导入,即可离线使用 ER 关系、图谱、对象管理、表列表等功能;导入与「导入配置」共用入口(数据源页导入/导出两个按钮),文件自动识别;导入前预检映射——按连接身份(与表格批量导入去重同口径)自动匹配本机数据源并预填,可改选或新建;结构缓存整粒度替换、标注数据按自然键合并且不覆盖本机已有的 ER 否决/确认决策;数据源正在同步元数据时拒绝导入

### 变更

- ER 图、字段明细「图谱」、对象管理图三处画布的 Shift 框选/多选统一收敛为底座能力:套索改为矩形框选、多次框选/点选可累加,「框选松手误清选中」「Shift+点击误触节点交互」等问题一并收口;鸟瞰图左侧新增半透明操作提示
- 图谱多选集合被外部清空(关多选面板/点节点开单表面板)时同步清除画布选中态,不再出现「下次 Shift+点击变取消选中」
- 授权签发工具(LicenseKeygen)支持直接粘贴带中文说明的功能列表,自动剥离说明、忽略未知 key 并去重,避免「license_admin(授权码管理)」整串写进授权码导致匹配不上
- 激活页在授权有效时自动进入系统,不再需手动点「进入系统」
- 数据源页「导入配置/导出配置(JSON)」按钮收敛为「导入/导出」(弹窗内切换连接配置/元数据缓存)
- 头栏布局微调:通知铃铛推到最右侧,主题切换按钮紧随其后

## 2.0.3 (2026-09-11)

### 新增

- ER 关系页新增「关系管理」对话框:整库关系清单一次拉全,状态筛选(默认仅候选,选项带计数)与表名关键字过滤在前端完成,勾选后可批量确认/批量否决/批量删除(单次上限 1000 条),删除不限状态并提示误删可重新推导找回
- ER 图节点点选与套索多选:点节点选中(锚点表恒为主题色底纹),Shift+点击增减,Shift+拖动套索框选;底部悬浮操作条可一键否决选中表的全部关系
- 字段明细「图谱」页签支持套索框选多节点,批量改色/改尺寸(自定义尺寸本地记忆)
- ER 关系推导断网降级:数据源不可达时只要本地有结构缓存仍能跑出候选(不再干等连接超时,不可达标记 5 分钟内连连接都不取),候选按「未验证」入库、交集率/置信度留空,完成对话框同屏警示待恢复后复核;本地完全没有缓存时任务明确失败并给出可操作提示

### 变更

- 「只用别名匹配」开关改为按字段生效:填了映射名的字段只用映射名搜索,未填映射名的字段仍按本名正常匹配,不再出现「开着开关却全部零命中」
- 关系删除不再限候选状态:确认/否决的关系也可删除(误删可重新推导找回)
- 图谱标记/颜色筛选由「降亮度高亮」改为「裁剪出图」,未命中节点直接移出图面;完全无标记的表按表名稳定取色(同表恒同色),改色即时生效不再整图重建
- 关系推导与元数据同步的调用失败日志由 warn 升级为 error 并带堆栈,便于定位真实错误

### 修复

- 修复超长元数据文本拖垮整批写入的问题:MySQL ENUM 完整类型串、超长注释/默认值会超出原列宽导致整批刷新/扫描失败,现将相关列放宽为长文本(只放宽类型、不截断不丢数据)
- 修复 Shift+拖动框选时画布跟着平移、框选轨迹飘动,以及框选把节点文字整片高亮的问题
- 修复套索松手后选中立刻被清空的问题
- 修复改色后画布先闪主题色再跳变的问题

## 2.0.2 (2026-09-11)

### 新增

- 数据源页「刷新」按钮:弹窗勾选数据源(默认全选)后后台批量同步——把能连上的数据源的库/schema/表/字段/索引等结构元数据全部写入本地缓存(已有缓存整粒度覆盖更新),连不上的数据源单独标记失败、不中断整批;同步后断网离线也能正常浏览这些库表结构
- SQL 控制台数据源下拉新增「本地 H2 库(只读)」:可直接查询应用自身配置库(前端合成的首项,不进数据源管理页),语句级只读校验(首关键字白名单 + H2 解析器只读判定),写语句一律拒绝
- 图谱新增调试面板(表详情「图谱」页签、对象管理「图谱」页签同款):实时调节节点尺寸、连线边长与粗细、文本透明度及力导参数(向心力/排斥力/连线吸引力),「重置」恢复默认并整体重绘

### 变更

- 图谱交互:鼠标悬停节点临时置顶,密集图中被压住的节点与标签悬停即可看清;单击节点区分圆形本体与标签文本(点标签不再误开节点面板)
- 对象管理「图谱」默认参数调定固化(目录/表节点尺寸、S1/S2 边长、连线粗细与文本透明度、力导三力),图谱更紧凑聚拢

### 修复

- 断网时点「继续扫描」不再把任务误判为失败,而是还原任务状态并提示恢复网络后再续扫
- 字段明细页在数据源不可达且无缓存时不再整页空白,其余区块(扫描统计/索引/标签)照常展示

## 2.0.1 (2026-09-11)

### 修复

- 图谱:修复连线盖住节点的问题——拖拽节点后 G6 会把被拖节点永久置顶,此后异步追加的关系边默认层级被抬上节点层,导致线压节点、点节点名称选不中;现在节点/连线显式固定层级,节点恒定在线之上,点名称即可选中
- 图谱:去掉节点中心与连线上的调试数字(尺寸/边长数值),图面更干净

## 2.0.0 (2026-09-10)

### 变更

- 数据预览页签「导出列表」改为服务端流式全量导出:导出符合已应用 WHERE 条件的全部数据并保持 ORDER BY(上限 20 万行静默截断),替代原先仅导出当前页
- 数据预览导出 xlsx 表头改为两行:第一行字段中文名称(取列注释,缺失回退英文列名)、第二行字段英文名称,不再拼接数据类型;预览表格表头同步叠加显示字段注释(缺失显示占位,悬停看全文)

## 1.9.9 (2026-09-07)

### 新增

- 局域网共享:UDP 广播自动发现同网段实例,可预览勾选互拉标记/表描述/表所属系统与全部扫描记录(数据源按连接特征自动映射)
- 局域网共享支持手动添加实例:广播发现不可用的网络可按 IP+端口直连探测添加,页面展示本机地址与端口供他人添加
- 头部导航新增通知铃铛:全局接口反馈通知全部留痕(localStorage 保存最近 100 条),抽屉内查看历史、一键清空
- 数据源「测试连接」升级为 DataGrip 风格详情:数据库产品/驱动名称与版本、连接耗时、SSL 是否启用、数据库兼容模式

### 变更

- 全局操作反馈由 ElMessage 改为右上角滑入的通知卡片(macOS 通知风格):标题+摘要两行,超长内容可原地展开全文,相同内容 2 秒内去重防刷屏
- 前端发版后旧页面点击报「资源加载失败」的问题:检测到懒加载资源失效自动整页刷新一次换新版本,仍失败则提示关闭窗口重开

## 1.9.8 (2026-09-06)

### 新增

- 更新日志:新版本首次打开自动弹出「本次更新」,点击页脚版本号可查看全部历史版本更新记录
- 批量打标:表列表勾选多表后一键批量打上选中标记(只增不删、幂等),打标弹窗内可就地新建标记
- 字段明细页新增「标签」页签:查看当前表已打标记,按 人工打标/系统打标 区分来源并支持筛选,可直接打标
- 字段明细页工具栏新增「采集/取消采集」按钮,无需返回表列表即可操作当前表
- 头部导航栏新增浏览器式 返回/前进/刷新 按钮;刷新为软刷新,不丢失页签状态
- 表列表新增「列设置」,可自定义显示列;表头固定、超出可横向滚动
- 库列表增加本地缓存:断网或目标库不可达时仍可正常浏览已缓存的库清单

### 变更

- 表标记新增「标记类型」:系统 / 可用于AI打标 / 仅用于人工打标;AI 自动打标只从「可用于AI打标」的标记中选择,新建标记默认仅用于人工打标,可在标记统计页修改类型
- 表列表「只看空表」复选框升级为「数据情况」下拉,可筛选 有数据表/无数据表
- 扫描记录导出/导入随任务携带「空表」系统标记关系,换机导入后历史记录不再丢失空表标记
- 标记与描述数据导出/导入新增携带标记类型与表所属系统(旧版本导出文件可正常导入)
