# 更新日志

## 2.0.6 (2026-09-14)

### 变更

- 交付形态调整:fat jar 变为**纯 API 服务**(`shadowJar` 排除 `static/**`,不再内嵌前端资源),Tauri 桌面端从本地 `web/dist` 直载页面(秒开,不再 navigate),API 走 `127.0.0.1` 动态端口;前端资源不再需要反复打开 75MB fat jar,缓解 Windows 杀软实时扫描导致的 assets 404
- jpackage 分发线(Windows 免安装 zip / macOS dmg / Linux)保留页面能力:打包脚本把 `web/dist` 放到应用镜像 `static/`,运行时经新增的 `-Ddq.web.static-dir` 从磁盘发前端

### 安全

- 新增浏览器访问管控 `dq.access-token`:配置后浏览器直接访问后端端口须携带令牌(请求头 `X-Dq-Token` / `?token=` query / Cookie 三选一命中),仅 `/api/health`、`/api/license/status`、`/api/lan/share/**` 免令牌;Tauri 每次启动随机生成并经 `-D` 注入,用户无感;令牌值不落日志、不回传接口
- 后端接入 CORS(`/api/*` 任意来源),仅用于 Tauri webview 跨域访问本机 API,配合上面的令牌门禁;`allowCredentials=false`

## 2.0.5 (2026-09-13)

### 新增

- 数据比对新增**对象匹配逻辑**(三方数据比对最容易踩的坑:对比表与基准表的身份字段对不上,同一条数据「code 一致 name 不一致」或「name 相同 code 不同」):新建向导第 2 步可选择三种判定「是否同一个对象」的口径 —— ① 编码+名称都相等(默认,最严格);② 先编码后名称(有编码先用编码配,编码没配上的对象再用对象名称配一轮);③ 编码/名称+大模型归一化(1、2 都没配上的残余对象交大模型按业务含义再认一轮,如简称/别名/错别字)。匹配逻辑 2/3 使用「对象编码」(比对主键)与「对象名称」两个字段,故选择后必须指定对象名称字段(前端拦下、后端 400);匹配逻辑 3 在未配置大模型、双侧残余超过 2000 条或模型调用失败时**自动降级**(残余按缺失/多余处理,原因写进目标说明),不会让整个任务失败。落库新增 `compare_job.match_mode`(V49,老任务为空 = 仅按编码对齐,历史结果口径不变)、`compare_target.code/name/ai_matched_count`(按来源统计命中数)与 `compare_diff.match_by`(CODE/NAME/LLM);差异明细里靠名称或大模型配上的对象打「名称配对 / AI 配对」标签,质量报告新增「对象命中」列(悬停展开编码/名称/大模型各自命中数),大模型调用按新场景「比对匹配」计入 AI 用量统计
- ER 关系人工审核留痕(重构要点):新增「原始关系」快照表 `table_relation_original`(V47 迁移),原始关系 = 大模型推导产出的关系、每次推导刷新而人工审核不改变它;最终关系 = 人工审核最后一次修改后的结果(`table_relation`,新增 `reviewed`/`reviewed_at` 标记人工已审核)。重新推导时人工已确认/已否决的关系**一律保留上次审核结论**(不再回炉为候选),只有从未审核的新关系才留给人工审核;迁移按「当前推导属性 + 状态 CANDIDATE」回填存量关系的原始快照,MANUAL 关系算人工新增
- 「导出ER关系 / 导出ER表格」改为导出 3 个 sheet,顺序固定为**表清单 / 推导关系清单 / 关系变化**,三者**同表头同数据逻辑**(库名/模式/表英文名称/表中文名称/数据量/级联字段编码/级联字段名称/标签;每表 × 该表作为关系端点的级联字段;有中心/锚点表时整表不列即不显示主表只显示副表):表清单取当前图、推导关系清单取大模型推导产出的 `table_relation_original`;关系变化**逐「表.字段」做差、只保留只在一侧出现的行**(两边都有的 表.字段 不算变化,不因同表另有字段差异而被带出),并在末列追加「备注」= 人工审核时填写的备注说明(人工否决原因即在此体现)

### 变更

- 关系批量处理对话框:「备注」列由只读文本改为**输入框**(人工填写否决原因,随本次「批量确认/批量否决」一并落库,只提交本次真正改过的行、未动的行保留原备注);移除「批量删除」按钮,只保留批量确认与批量否决两个按钮(逐条删除仍在关系详情抽屉,接口 `DELETE /api/relations/{id}` 与 `POST /api/relations/batch-delete` 保留)

- 数据比对的「导出差异表」改为按客户既有核对表格式导出「比对报告」(文件名 `比对总览-{任务id}.xlsx`,按钮文案同步改为「导出比对报告」):首个 sheet 固定为「总览」,列口径为 表中文名(表注释)/表英文名称/所属系统(table_system 登记值,未登记回落数据源名)/条数(该侧表实际行数,目标侧含多余行)/数据最新更新时间(当前未采集,固定「—」占位)/与基准差(目标行数 − 基准行数)/匹配编码数(SAME+DIFF)/差异条数(缺失+多余+不一致对象)/差异原因(按差异构成自动拼写,可导出后人工补充),第一行为基准表本身,其后一行一个比对目标
- 明细 sheet 采用精简版(对齐客户核对表):第 1 行单行上下文、第 3 行表头(字段注释做中文列名 + 末列「说明」)、其后一行一个差异对象且一格一个对象;为此 `compare_diff.diff_json` 改为整行快照(DIFF 行每个比对字段各一条、value 仅不一致字段非空,MISSING/EXTRA 单侧齐备),前端展示与问题字段排行口径不变,老任务仍可正常展示
- 总览之后**总览里有几条差异数据就跟着几个明细 sheet**(即一个比对系统一张,sheet 名 `序号_数据源名`,序号与总览行一一对应):每个 sheet 装该系统**全部**差异,粒度为字段级——说明列写清差异(DIFF 行形如 `字段: 基准值 → 目标值`,缺失/多余写「基准有目标无」「目标有基准无」);明细行数超 100 万行(Excel 单表上限兜底)时截断并标注
- 比对目标新增实际总行数落库(V44 迁移 `compare_target.target_count`),供总览「条数/与基准差」使用;历史任务无该值时用「匹配数 + 多余数」兜底
- 侧边栏数据源的编辑图标改为就地弹编辑框:在任意页面点该图标都直接弹出当前页的数据源编辑框,不再先跳到数据源页再弹(弹框组件移到 App.vue 持有,侧边栏行与数据源页卡片同源);保存后广播的列表变更会让侧边栏与当前数据源页卡片一起刷新
- 库过滤口径前后端统一:数据源未配置库白名单时,接口按「默认规则」只返回业务库(排除各方言系统库),与「库过滤」页签里系统库默认不勾选完全一致 —— 之前是「未配置 = 不过滤 = 系统库照样返回」,页面上系统库没勾选、查询库列表却又把系统库带回来;需要在库列表/扫描/选库里包含系统库时,在库过滤页签勾上并保存(写入显式白名单)即可。「全勾/全不勾 = 不过滤」的提交口径同步收敛到前端 `utils/schemaFilter.js`,避免「全勾(含系统库)」被默认规则吃掉
- ER 图「关系管理」对话框补齐表的筛选:新增按表标记多选筛选(关系的两端表任一命中即展示,下拉只列当前清单里实际用到的标记)与表格「标记」列(两端标记合并去重,空表标记浅色区分);原「按表名过滤」升级为关键字搜索,一次命中两端表名、表注释、两端字段名与备注;三项过滤仍在纯前端完成,切换不回源,`GET /api/relations` 接口保持不变
- ER 图「关系管理」对话框列结构调整:列名改为「主表 / 副表」,**中心表恒排在主表列**(它在库里可能本身处于 many 侧,旧实现直接照搬 one/many 顺序会把它排到第二列),两列统一为「第一行中文表名 + 第二行 `表名 [字段名]`」(不再用点号连接),取消单独的「多端字段」列;主表列行尾右对齐、副表列行首左对齐标注重数 `1`/`N`,按存储方向还原(中心表在重复侧时主表标 N、副表标 1,1:1 与疑似多对多两端均为 `1`),两端标记与表标记同款圆角标签但 `N`(多端)用橙色、`1`(唯一端)用主题蓝,两列各自显示本端真实重数(展示行的基数简称 `card` 由接口的 `cardinality` 归一而来,1:N 关系中心表在重复侧时主表标 N、副表标 1,1:1 两端均为 1),基数由两端标记表达,故「关系/基数」列整列删除
- ER 图「关系管理」对话框清单范围与默认口径调整:表详情页 ER 关系页签入口改为**接口只拉本表参与的关系**(`GET /api/relations?table=` 两端任一命中),不再拉整库再前端过滤——清空弹窗上全部筛选条件后,列表呈现的仍是该中心表的完整相关关系;状态筛选默认由「仅候选」改为「全部」,星型图入口也不再预填关键字(状态切「仅候选」、输关键字仍是可选收窄手段);ER 关系页全库总图入口保持整库清单

### 修复

- 修复数据比对向导第 4 步「字段映射」画布的一串问题:**连线渲染错位**(mapping 模式端点计算把节点中心又加了一次半宽半高,线全部画到卡片右下方、看起来像没连线——已按 getCenter() 统一口径修掉);**点击连线经常没反应**(create-edge 临时边 id 与已存在的正式映射边撞 id,G6 抛 Edge already exists 中断交互、橡皮筋残留,已改唯一临时 id、由父级按映射重建收敛);**鼠标悬停很卡**(hover 高亮原来走整图 setData+draw 重绘,改为直接改行 DOM,换行才动一次手);**初始视口内容飞出屏幕**(fitCenter 聚焦的是画布包围盒而非图内容,改为按节点并集包围盒直接落相机位姿);字段映射画布**字段全量展开**(不再「+N 个字段」折叠,目标行恒可点);进入画布时**不再自动按名称连线**(画布空白由用户人工连线,「按名称自动匹配」按钮一键填充,口径不变);顺带修复画布底座 fit='center' 的居中口径(表详情 ER 页签同样受益)与鸟瞰图对 create-edge 辅助节点的 cloneNode 报错
- 修复数据源编辑对话框「库过滤 → 加载库列表」对 MySQL 等单库方言提示「目标库没有可选择的库」、已选 0/0 的问题:回源取库清单时只调了 `listDatabases`(单库方言未实现、恒为空),空值又把本地库清单缓存连同同一批 schema 行一起覆盖成空;现按方言统一口径 `listDatabasesOrSchemas`(单库方言回落 schema 列表),缓存回源与「按表单连接参数实时拉取」两条路径共用同一实现
- 修复头栏「刷新」(软刷新)后侧边栏数据源 ⋮ 菜单的「新增/导入/导出」点击没反应:软刷新换 keep-alive key 后,旧的数据源页实例仍留在缓存里,它的 watcher 抢先消费了侧边栏写入的待办标记,弹窗开在已停用的隐藏子树上,当前可见实例什么都不显示;现改为停用实例不消费标记、页面激活时补一次消费

## 2.0.4 (2026-09-13)

### 新增

- 数据比对(受控功能,授权码需显式包含 compare):选定一张基准表,与多个目标系统的对应表按比对主键逐字段比对,产出差异明细(完全一致/字段不一致/对象缺失/对象多余四类,字段级逐字段展开)与质量报告(对象覆盖率、字段一致率、数据完整率、综合评分四比率 + 问题字段排行);差异明细支持按类型/厂商/关键字筛选并导出 Excel;后台执行带阶段进度,单目标失败不中断整批,支持重新比对、归档、删除;数值字段自动归一(千分位/小数尾零),NULL 与空串视为一致,单侧上限 50 万行
- 数据源页新增「导出元数据」:把数据源的元数据缓存(库/表/字段/索引/DDL 等结构缓存)连同派生标注数据(表说明、表标记、ER 关系、数据目录等)打包成 JSON 文件;现场人员「刷新」元数据后导出,交给连不上业务库的人导入,即可离线使用 ER 关系、图谱、对象管理、表列表等功能;导入与「导入配置」共用入口(数据源页导入/导出两个按钮),文件自动识别;导入前预检映射——按连接身份(与表格批量导入去重同口径)自动匹配本机数据源并预填,可改选或新建;结构缓存整粒度替换、标注数据按自然键合并且不覆盖本机已有的 ER 否决/确认决策;数据源正在同步元数据时拒绝导入

### 变更

- ER 图、字段明细「图谱」、对象管理图三处画布的 Shift 框选/多选统一收敛为底座能力:套索改为矩形框选、多次框选/点选可累加,「框选松手误清选中」「Shift+点击误触节点交互」等问题一并收口;鸟瞰图左侧新增半透明操作提示
- 图谱多选集合被外部清空(关多选面板/点节点开单表面板)时同步清除画布选中态,不再出现「下次 Shift+点击变取消选中」
- 授权签发工具(LicenseKeygen)支持直接粘贴带中文说明的功能列表,自动剥离说明、忽略未知 key 并去重,避免「license_admin(授权码管理)」整串写进授权码导致匹配不上
- 激活页在授权有效时自动进入系统,不再需手动点「进入系统」
- 数据源页「导入配置/导出配置(JSON)」按钮收敛为「导入/导出」(弹窗内切换连接配置/元数据缓存)
- 头栏布局微调:通知铃铛推到最右侧,主题切换按钮紧随其后

## 2.0.3 (2026-09-11)

### 新增

- ER 关系页新增「关系管理」对话框:整库关系清单一次拉全,状态筛选(默认仅候选,选项带计数)与表名关键字过滤在前端完成,勾选后可批量确认/批量否决/批量删除(单次上限 1000 条),删除不限状态并提示误删可重新推导找回
- ER 图节点点选与套索多选:点节点选中(锚点表恒为主题色底纹),Shift+点击增减,Shift+拖动套索框选;底部悬浮操作条可一键否决选中表的全部关系
- 字段明细「图谱」页签支持套索框选多节点,批量改色/改尺寸(自定义尺寸本地记忆)
- ER 关系推导断网降级:数据源不可达时只要本地有结构缓存仍能跑出候选(不再干等连接超时,不可达标记 5 分钟内连连接都不取),候选按「未验证」入库、交集率/置信度留空,完成对话框同屏警示待恢复后复核;本地完全没有缓存时任务明确失败并给出可操作提示

### 变更

- 「只用别名匹配」开关改为按字段生效:填了映射名的字段只用映射名搜索,未填映射名的字段仍按本名正常匹配,不再出现「开着开关却全部零命中」
- 关系删除不再限候选状态:确认/否决的关系也可删除(误删可重新推导找回)
- 图谱标记/颜色筛选由「降亮度高亮」改为「裁剪出图」,未命中节点直接移出图面;完全无标记的表按表名稳定取色(同表恒同色),改色即时生效不再整图重建
- 关系推导与元数据同步的调用失败日志由 warn 升级为 error 并带堆栈,便于定位真实错误

### 修复

- 修复超长元数据文本拖垮整批写入的问题:MySQL ENUM 完整类型串、超长注释/默认值会超出原列宽导致整批刷新/扫描失败,现将相关列放宽为长文本(只放宽类型、不截断不丢数据)
- 修复 Shift+拖动框选时画布跟着平移、框选轨迹飘动,以及框选把节点文字整片高亮的问题
- 修复套索松手后选中立刻被清空的问题
- 修复改色后画布先闪主题色再跳变的问题

## 2.0.2 (2026-09-11)

### 新增

- 数据源页「刷新」按钮:弹窗勾选数据源(默认全选)后后台批量同步——把能连上的数据源的库/schema/表/字段/索引等结构元数据全部写入本地缓存(已有缓存整粒度覆盖更新),连不上的数据源单独标记失败、不中断整批;同步后断网离线也能正常浏览这些库表结构
- SQL 控制台数据源下拉新增「本地 H2 库(只读)」:可直接查询应用自身配置库(前端合成的首项,不进数据源管理页),语句级只读校验(首关键字白名单 + H2 解析器只读判定),写语句一律拒绝
- 图谱新增调试面板(表详情「图谱」页签、对象管理「图谱」页签同款):实时调节节点尺寸、连线边长与粗细、文本透明度及力导参数(向心力/排斥力/连线吸引力),「重置」恢复默认并整体重绘

### 变更

- 图谱交互:鼠标悬停节点临时置顶,密集图中被压住的节点与标签悬停即可看清;单击节点区分圆形本体与标签文本(点标签不再误开节点面板)
- 对象管理「图谱」默认参数调定固化(目录/表节点尺寸、S1/S2 边长、连线粗细与文本透明度、力导三力),图谱更紧凑聚拢

### 修复

- 断网时点「继续扫描」不再把任务误判为失败,而是还原任务状态并提示恢复网络后再续扫
- 字段明细页在数据源不可达且无缓存时不再整页空白,其余区块(扫描统计/索引/标签)照常展示

## 2.0.1 (2026-09-11)

### 修复

- 图谱:修复连线盖住节点的问题——拖拽节点后 G6 会把被拖节点永久置顶,此后异步追加的关系边默认层级被抬上节点层,导致线压节点、点节点名称选不中;现在节点/连线显式固定层级,节点恒定在线之上,点名称即可选中
- 图谱:去掉节点中心与连线上的调试数字(尺寸/边长数值),图面更干净

## 2.0.0 (2026-09-10)

### 变更

- 数据预览页签「导出列表」改为服务端流式全量导出:导出符合已应用 WHERE 条件的全部数据并保持 ORDER BY(上限 20 万行静默截断),替代原先仅导出当前页
- 数据预览导出 xlsx 表头改为两行:第一行字段中文名称(取列注释,缺失回退英文列名)、第二行字段英文名称,不再拼接数据类型;预览表格表头同步叠加显示字段注释(缺失显示占位,悬停看全文)

## 1.9.9 (2026-09-07)

### 新增

- 局域网共享:UDP 广播自动发现同网段实例,可预览勾选互拉标记/表描述/表所属系统与全部扫描记录(数据源按连接特征自动映射)
- 局域网共享支持手动添加实例:广播发现不可用的网络可按 IP+端口直连探测添加,页面展示本机地址与端口供他人添加
- 头部导航新增通知铃铛:全局接口反馈通知全部留痕(localStorage 保存最近 100 条),抽屉内查看历史、一键清空
- 数据源「测试连接」升级为 DataGrip 风格详情:数据库产品/驱动名称与版本、连接耗时、SSL 是否启用、数据库兼容模式

### 变更

- 全局操作反馈由 ElMessage 改为右上角滑入的通知卡片(macOS 通知风格):标题+摘要两行,超长内容可原地展开全文,相同内容 2 秒内去重防刷屏
- 前端发版后旧页面点击报「资源加载失败」的问题:检测到懒加载资源失效自动整页刷新一次换新版本,仍失败则提示关闭窗口重开

## 1.9.8 (2026-09-06)

### 新增

- 更新日志:新版本首次打开自动弹出「本次更新」,点击页脚版本号可查看全部历史版本更新记录
- 批量打标:表列表勾选多表后一键批量打上选中标记(只增不删、幂等),打标弹窗内可就地新建标记
- 字段明细页新增「标签」页签:查看当前表已打标记,按 人工打标/系统打标 区分来源并支持筛选,可直接打标
- 字段明细页工具栏新增「采集/取消采集」按钮,无需返回表列表即可操作当前表
- 头部导航栏新增浏览器式 返回/前进/刷新 按钮;刷新为软刷新,不丢失页签状态
- 表列表新增「列设置」,可自定义显示列;表头固定、超出可横向滚动
- 库列表增加本地缓存:断网或目标库不可达时仍可正常浏览已缓存的库清单

### 变更

- 表标记新增「标记类型」:系统 / 可用于AI打标 / 仅用于人工打标;AI 自动打标只从「可用于AI打标」的标记中选择,新建标记默认仅用于人工打标,可在标记统计页修改类型
- 表列表「只看空表」复选框升级为「数据情况」下拉,可筛选 有数据表/无数据表
- 扫描记录导出/导入随任务携带「空表」系统标记关系,换机导入后历史记录不再丢失空表标记
- 标记与描述数据导出/导入新增携带标记类型与表所属系统(旧版本导出文件可正常导入)
