# save_download 命令的应用层权限(ACL 强制校验的原因见 report.toml 头注)。
[[permission]]
identifier = "allow-save-download"
description = "允许调用 save_download 命令(通用下载「直存数据目录」:GET 本地后端流式导出接口,写数据目录/exports/,不弹保存框)"
commands.allow = ["save_download"]
